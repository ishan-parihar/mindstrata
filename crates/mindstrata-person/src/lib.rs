//! Embodied agent substrate: biological systems and the person aggregate.
//!
//! Extracted from `mindstrata-sim` as the first leaf crate (S2 of
//! `docs/PLAN_SCALING_FOUNDATION.md`). Depends only on `mindstrata-core`.

#![deny(missing_docs)]

pub mod biology;
pub mod person;
