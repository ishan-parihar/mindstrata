# Mindstrata — Remaining Work Report

**Scope:** refinement, testing, validation, and optimization items still open.
**Baseline:** commit `086f73e` (Iteration 134), tree clean, **1,105 workspace tests green** (849 sim + 236 tests-crate + 20 core), zero snapshot drift, strict clippy `-D warnings` clean.
**Plan status:** AP2 is structurally ~99% complete — every plan-mandated *system* exists and is wired into the deterministic tick loop. The remaining work is overwhelmingly **decisional consumers** (state that is produced but never read back into decisions), **scale/perf hardening**, and **post-AP2 game features**.

---

## 1. Refinement — Decisional Consumers (write-only → behavioral)

The dominant category. A recurring pattern across the campaign: systems were built and wired as **observational state** (pure, no RNG, excluded from golden projections → zero drift), with their *behavioral consumer* documented as "future work". Wiring any of these **will re-calibrate the golden baseline** (each is a separate, carefully-tested iteration — the Iter-66 precedent).

| # | Plan § | Item | Producer (iteration) | Status |
|---|--------|------|----------------------|--------|
| 1 | §7.2.6 | **WIRED (Iter 92)** — the conception→pregnancy→birth pipeline is live: `should_birth` starts a pregnancy on the female partner, gestation advances per-tick, and the birth pass is keyed off the pregnancy (widow births deliver; same-sex couples keep the legacy immediate-birth path). `Marriage.children` now populated at birth | Iter 42 | Wired |
| 2 | §8.1.4 | **PARTIAL (Iter 123)** — six channels live: loneliness→social-seeking (Iter 98, `interact_chance` + loneliness×0.3), tenderness→helping (Iter 99, `help_propensity` + tenderness×0.5), humiliation→failed-threat escalation amplifier (Iter 116, `should_escalate` × (1 + humiliation×0.3)), contempt→failed-threat escalation amplifier (Iter 122, `should_escalate` × (1 + contempt×0.3); ONE-SIDED identity-at-zero), despair→failed-threat escalation pacifier (Iter 122, `should_escalate` × (1 − despair×0.5) floored at 0.5; ONE-SIDED identity-at-zero), envy→courtship-interest aversion (Iter 123, `attraction.envy_cost` = live `emotions.envy` × 0.2 negative; ONE-SIDED identity-at-zero; distinct from Iter-113 peer-status `envy_apply` (anger channel) — that is relational-field status frustration, this is the appraisal coveting emotion). Iter 123 also FIXED the Iter-78 disgust channel: it read the emptied agent emotion field (the tick `std::mem::take` parallel-array pattern) — a latent dead channel — now reading the live local vec (still zero-blast: disgust 0 in calm windows). Iter 116 added the per-tick proportional decay (0.12/tick). **COMPLETE (Iter 130)** — all 12 expanded-family §8.1.4 emotions wired: loneliness→social-seeking (98), tenderness→helping (99), humiliation→escalation (116), contempt→escalation (122), despair→escalation pacifier (122), envy→courtship aversion (123), disgust fixed (123), moral_outrage→escalation (125), gratitude→help (127), relief→escalation (128), nostalgia→collective-memory preservation (129), awe→scandal-damage reverence (130).   **Iteration 130 (fourth and FINAL CALIBRATED §8.1.4 consumer)**: awe → §11.1 scandal-damage reverence — `awe_reverence_factor` (1 − awe × 0.15, floored at 0.85, ONE-SIDED identity-at-zero) multiplies the daily witnessed-violation severity in the moral-cognition `scandal_damage` call ("reverence forgives", the emotional counterweight to the monthly ritual boost). Producer `|future_implication| × identity_relevance × (1 − expectedness)` — U-SHAPED on |future_implication| (fires on significance in either direction: wonder OR dread); live (mean 0.55–0.65). WIRING DISCOVERY: awe converges to its world equilibrium (~0.67) before scandal erosion begins (~tick 120), so the sim wiring test uses a manual-tick re-injection harness (pinned factor 0.85 vs natural 0.899, scandal-site probe) — liveness proven by input-pinning (the weakest observability of the set). ZERO baseline blast (consumer-side: erosion floors legitimacy at 0, 0.5-anchored grievance/theft consumers never activate, motivation-context shift below granularity): golden + 14 snapshots byte-identical, NO regeneration. 849 sim + 226 integration green.  **Iteration 129 (third CALIBRATED §8.1.4 consumer)**: nostalgia → §13.5 collective-memory preservation — `nostalgia_preservation_factor` (1 − nostalgia × 0.3, floored at 0.7, ONE-SIDED identity-at-zero) scales the daily collective-memory salience decay via `tick_all_preserved` (population mean nostalgia, deterministic). Producer `narrative_meaning × positive` (meaningful past) — live (mean 0.78–0.88, max 0.88). FLOOR-PINNED: the daily pass reads the emotion field where nostalgia saturates at 1.0 → factor pins at exactly 0.7 — a UNIFORM 30% slowdown in calibrated windows, NOT a differential consumer (near-saturation dead-signal risk applies; stress/famine worlds would show the differential). ZERO baseline blast by construction (collective-memory salience not in golden/snapshots — verified): golden + 14 snapshots byte-identical, NO regeneration; wiring proven by the sim-level decay-delta test (decayed < un-scaled AND |decayed − floor| ≤ 0.0002 pins the factor at the floor). 847 sim + 225 integration green.  **Iteration 128 (second CALIBRATED §8.1.4 consumer)**: relief → §19.5.H escalation amplifier — `relief_escalation_factor` (1 + relief × 0.1, factor 1.10 at full, ONE-SIDED identity-at-zero, NO clamp) multiplied into the `should_escalate` chance chain after the outrage factor and before the despair pacifier; the emboldened-survivor counterpoint to the Iter-122 despair pacifier. Producer `positive × (1 − controllability)` (lucky escape) — live in RECOVERY windows (mean > 0.5 @5000), dormant in famine/stress windows → SELECTIVE: golden + all 14 snapshots byte-identical with NO regeneration; the only observable emergent shift is the seed-42 panic fire 17713→18577 (re-anchored [18400,18700]) and the stress-conflict statistical band 0.05→0.07 (probe 0.357 vs 0.417, documented re-pin). TUNED from an initial 0.2 rate whose probe showed a disproportionate blast (panic 1408, 12.5× earlier; kin seed-42 inversion; famine +7 early mortality) — the escalation chain has strong violence→stress feedback, so a live emotion multiplies beyond its face value; 0.1 is the calibrated sweet spot. 844 sim + 224 integration green. **Iteration 127 (first CALIBRATED §8.1.4 consumer)**: gratitude → Help propensity — the fold extracted into the public pure helper `appraisal::help_propensity(norm, tenderness, gratitude, m_tend, m_grat)` (clamped, identity at zero) and wired into the agent_info 7th element (destructures as `help_neighbors_propensity` → widens the Help window `[0.2, 0.5×(1+p))` in high-affection pairs); `social_gratitude_help_multiplier` 0.5 (tenderness tier). Producer live (mean > 0.1 @5000). CALIBRATED: golden + 6 snapshots regenerated (before/after evidence); 4 trajectory tests re-anchored (famine peak 1100→1000 twin-peaked, panic fire 13537→17713, births [31070]→[78470] with the 1-chain intact); 842 sim + 223 integration green. **Iteration 126 queue correction**: jealousy was listed as unwired but is PROVABLY WIRED — the appraisal producer (status_threat × attachment_threat, appraisal.rs:362) → daily `charge_jealousy` (sim.rs:7041) → jealousy_load → strain (`record_negative` > 0.6) → `should_dissolve` (sim.rs:7070) → marriage dissolution (Separation), all production-active since the §10.4 bond era; probe pins emotion AND load at exactly 0 in all calibrated windows (both producer inputs dormant in calm worlds — the chain is ARMED and live, inert only because its inputs never fire) — zero-blast, golden + 14 snapshots byte-identical, NO regeneration; 841 sim + 222 integration green. **Iteration 125**: moral_outrage → failed-threat escalation amplifier (`should_escalate` × `moral_outrage_escalation_factor`, `1 + outrage × 0.3`, factor 1.30 at full — the THIRD amplifier on the §19.5.H decision after humiliation/contempt, from the righteous-indignation layer, the AP2 §8.1.10 honor-feud path; NO clamp per the Iter-112 lesson). The zero is ARMED-not-absent: sacred values live (mean 0.55–0.63) AND goal_relevance live, and the producer `witnessed_unfairness × max_sacredness × goal_relevance` fires at ANY positive unfairness (no 0.05 gate on this channel — that threshold gates only the `fairness` field sign), so the exact-zero probe pins `witnessed_unfairness == 0` exactly (calm worlds witness no injustice). ONE-SIDED identity-at-zero; ZERO-blast — golden + 14 snapshots byte-identical, NO regeneration; 841 sim + 221 integration green. Iter 124 FIXED the §8.1.5 motivation-context dead channel: sim.rs:2510-2513 read the EMPTIED agent emotion field (the tick `std::mem::take` parallel-array pattern) into `MotivationState::set_context`, so the pressure boosts (`1 + fear×0.4` Safety, `1 + anger×0.4` Justice, `1 + joy×0.3` Romance, `1 − sadness×0.3` Meaning) ran with an emotionless context since the parallel-array refactor; now reads the live local vec — surprisingly ZERO-blast (probe: motivation.fear 0.0000 → 0.83–0.999 across seeds/horizons, yet golden + snapshots byte-identical: Safety stays dominant and the Iter-96 exclusive boost scales uniformly). QUEUED CALIBRATION: the 8 base emotions lack the Iter-116 proportional decay (fear mean 0.83–1.0 in calm worlds) — core-emotion decay queued to restore amplification differentiation | Iter 124 | Partially wired |
| 3 | §9.2 | **WIRED (Iter 94)** — `learned_delta` (normalized per-profile dot of learned values vs the 0.5 prior) feeds `compute_utility`; the RL loop is closed (learning shapes action choice; zero at the prior → tick-0-identical) | Iter 51 | Wired |
| 4 | §8.1.16 | **WIRED (Iter 103) for the dread channel** — scenario-grounded dread feeds `compute_utility` (precautionary provisioning: Work/Trade +0.2·dread, Rest −0.1·dread; zero-at-zero; deterministic). Both D1 scarcity AND D2 threat negative scenarios ground dread (`evaluate_scenarios` sums negative-valence weights), so D2 threat reaches decisions via dread (probe-pinned: seed 42 fires D2 from ~1000 ticks — golden dread 0.288; the old "scarce world fires only D1" note was stale). **Iteration 117**: `ScenarioKind` discriminator (D1–D6) stamped per domain in `generate_daily_scenario` — the concern domains were previously indistinguishable; the plan's four emotion-bias lines verified wired (fear→dread, ambition→hope, trauma→catastrophe, depression→hope). D3–D6 direct decision folds: ATTEMPTED AND REVERTED (Iter-117 draft) — the probe premise that only D1/D2 fire in calibrated windows is FALSE (D3/D4/D5 fire in larger/longer worlds; 4 gate failures), and AP2 §8.1.16 prescribes emotion-biased prospection rather than domain→action folds; D3–D6 remain observational by design | Iter 71 | Wired (D1/D2 via dread) + discriminator |
| 5 | §8.1.5 | **WIRED (Iter 96)** — `dominant_need` urgency boost in `compute_utility`: the full-pressure argmax biases selection toward the dominant need's relief channel (exclusive boost; Safety/Esteem dominant = zero boost = fight-or-flight override) | Iter 44 | Wired |
| 6 | §8.1.2 | **WIRED (Iter 97)** — `percept_bias_factor` folds extraversion/openness/fear/anger/trauma into `compute_salience`; memory-encoding salience now reflects the agent's biases (social-memory encoding ~2× extravert-vs-introvert) | Iter 41 | Wired |
| 7 | §8.1.11 | **WIRED (Iter 95)** — `resolve_effect` applied at the live speech-act site: obligation→listener debt, reputation→listener admiration, status→speaker respect (trust/affection channels deliberately skipped — the interaction system already applies byte-identical deltas) | Iter 40 | Wired |
| 8 | §8.1.3 | **WIRED (Iter 104)** — `identity_relevance` derived at encoding (trauma 0.6 / social-emotional 0.3 / flashbulb +0.15 / milestones +0.2 / charge×0.1) and consumed by `retrieval_score` (× 0.25): the identity-protection recall bias is live — identity-salient traces intrude, rehearse, and survive eviction. Decision-level consumers (Episodic→narrative) remain future | Iter 43 | Wired |
| 9 | §8.1.6 | **WIRED (Iter 105)** — the plasticity pass was already running daily (sim.rs:3815, drifting each agent's Temperament layer), but the layer had **zero consumers** — write-only observational state. Now: the production appraise site multiplies fear/anger deltas by `Temperament::reactivity_amplifier(deviation)` where deviation = live reactivity − `from_traits` baseline (zero at construction → byte-identical until the pass drifts it). Differential-proven live (low-coping world, +42% fear at tick 5). Remaining: the 12 core traits themselves still never move (core-trait movement is a future, separately-calibrated iteration); sociability/boldness/diligence have no decision consumer yet | Iter 105 | Wired |
| 10 | §10.2 / §11.2 | **WIRED (Iter 102)** — directed `power_balance` fed into `should_escalate` as a multiplicative dominance scale (0.5× subordinate → 1.5× dominant, identity at zero → legacy byte-identical); the violence-escalation decision now reads the full relational-power stack. **Iteration 106**: `institutional_rank` now weighted into `effective_status()` (× 0.1, zero-at-zero; behavioral via §10.9 patronage — first big-blast iteration, golden + snapshots regenerated, 10 intent-preserving test recalibrations). **Iteration 107**: the sensory field's `perceived_stress` now has a decisional consumer — the daily fear-contagion fold (`FEAR_CONTAGION_RATE` 0.05, shared `contagion_apply` helper, zero-at-zero, deterministic). **Iteration 108**: the social field's `kin_count` now has a decisional consumer — the kin-support stress buffer (`KIN_STRESS_RATE` 0.15/kin, `KIN_STRESS_CAP` 0.45, `kin_stress_factor` scaling the §22.1 cognitive stress input; zero-at-zero → golden byte-identical, NO regeneration needed). **Iteration 109**: the noospheric field's `belief_confidence` now has a decisional consumer — the dogmatism channel (`BELIEF_CONFIDENCE_RIGIDITY` 0.5, `belief_rigidity_factor = 1 + confidence × rigidity` dampening the evidence term in `update_belief(s)` via `× (2 − rigidity_factor)`; evidence-dampening pivot: scaling resistance up amplifies positive updates; zero-blast — belief state decision-independent at calibrated horizons → golden byte-identical, NO regeneration needed). **Iteration 110**: the social field's `social_trust` now has a decisional consumer — the violence-pacification channel (`SOCIAL_TRUST_PACIFY_RATE` 0.3/unit, `SOCIAL_TRUST_PACIFY_CAP` 0.6, `trust_pacify_factor` scaling the `should_escalate` chance; identical-RNG unit proof + 4-leg integration test; BIG-BLAST iteration — golden + 6 snapshots regenerated, 5 intent-preserving re-pins). **Iteration 111**: the noospheric field's `legitimacy_perceived` now has a decisional consumer — the theft-deterrence channel (one-sided `legitimacy_deterrence_factor` anchored at the 0.5 construction value, scaling the `enforce_theft` amount; deterministic take-proof unit test 0.12 vs 0.15 + 3-leg integration test; ZERO-blast — legitimacy decays to ~0.04 in every calibrated window AND theft never fires → golden byte-identical, NO regeneration needed). **Iteration 112**: the noospheric field's `collective_fear` now has a decisional consumer — the moral-panic legitimacy-damage amplifier (one-sided `collective_fear_panic_amplifier` anchored at 0.95 ABOVE the calibrated peak 0.903, scaling `MoralPanicResult::legitimacy_damage` when the §7.2 trigger fires; a shipping unit test caught the initial `clamp_01` silently erasing the amplification — factor now provably ∈ [1.0, 1.5] without a clamp; deterministic differential unit test 0.3268 vs 0.34 + 3-leg integration test; ZERO-blast — collective fear peaks at 0.903 < 0.95 anchor and the single seed-42 panic sits at 0.90 → golden byte-identical, NO regeneration). The ENTIRE §10.1 three-field layer now has decisional consumers. **Iteration 113**: the social field's `peer_status` now has a decisional consumer — the daily envy-anger fold (one-sided `envy_delta`/`envy_apply` anchored at 0.5 ABOVE the probe-pinned ceiling of 0.4687 across the widest sweep yet — 4 seeds + drought/pestilence + 20K horizon; identity below → ZERO-blast, golden byte-identical, NO regeneration; the anger channel is decisional via threat_level/stress/witnessed-violations; shared-helper pattern + 4-leg integration test whose Leg D proves the fold is wired end-to-end). **Iteration 114**: the social field's `social_obligation` now has a decisional consumer — the escalation-restraint pacifier (one-sided `obligation_restraint_factor` at the semantic-midpoint anchor 0.5, scaling `should_escalate` right after Iter-110's trust; a wide-sweep probe the reviewer caught showed obligation has NO hard ceiling — 0.69@20K, scenarios 0.52–0.61@3000 — so the honest claim is identity in every golden/snapshot horizon (seed-42 max 0.456@5000, byte-identical gates) with deep-debt worlds engaging the restraint by design; identical-RNG wiring proof + 3-leg integration test; ZERO-blast, golden byte-identical, NO regeneration). **Row #10 COMPLETE — the ENTIRE §10.1 relational-field layer (sensory + social + noospheric) now has decisional consumers** | Iter 39/81/55/106/107/108/109/110/111/112/113/114 | Wired — COMPLETE |
| 11 | §10.4 | **WIRED (Iter 101)** — the daily courtship ladder is attraction-gated: `try_advance` now receives the pursuer's live `total_attraction()` (the 0.6× floor genuinely binds below it; the interaction path was already gated). Same-test differential pinned: suppressed village stalls below control (ceiling 0.163 vs 0.477). `taboo_penalty` still unwired; the D4 scenario gate remains the other consumer | Iter 75–79 | Wired |
| 12 | §10.4 | **WIRED (Iter 118)** — the "Seek proximity" step: the daily courtship pass walks the pursuer one deterministic Manhattan step/day toward the pursued when the pair sits beyond perception radius (`seek_step`, pure, no RNG), so courtships formed at distance converge into interaction range and the ladder un-stalls (probe: seed-42 pair 8→4 @1000, stage Betrothal @5000; the pair previously rotted at Awareness at distance 8). FULL-BLAST: golden + 5 snapshots regenerated, 6 trajectory tests re-pinned (panic emergence 3034→13537 re-anchored to 15,000 horizons; famine window trough→peak at 1100; friendship/stress correlations got 0.05 tolerance bands; birth pipeline re-pinned to [31070] single birth) | Iter 76 | Wired — FULL-BLAST |
| 13 | §10.7 | **WIRED (Iter 119)** — household food pooling: multi-member households only (singleton households skipped — identity by construction, and every calibrated window is all-singleton, probe-pinned); well-fed adults (Head/Partner/Adult) contribute their surplus below the 0.35 feed threshold to the communal pot (`pool_food` — division of labor), then hungry members are fed dependents-first (children/elders get the full 0.1 ration, adults the residual 0.05 half-ration — childcare/elder care) via `distribute_food`; exact relief `min(hunger, ration)`, pool never negative, no RNG (replay-deterministic). ZERO-BLAST: golden + 14 snapshots byte-identical, NO regeneration; 826 sim + 215 integration green; 6-leg integration test (exact-math, dependents-first ordering, singleton no-op, all-singleton population no-op, elder care, pool exhaustion). `pool_food`/`distribute_food` got their first production call sites and `HouseholdRole` its first consumer | Iter 53 | Producer-side only → WIRED (zero-blast) |
| 14 | §13.1–13.6 | **`narrative_dominance` → cluster assignment WIRED (Iter 120)** — the daily pro-institution heuristic now adds a narrative momentum bonus (max dominance > 0.6 anchor → `(max − threshold) × 0.25`, zero at/below → identity in every calibrated world, probe-pinned ceiling 0.52 across 9 seed×horizon combos to 20K); deliberately one-sided bandwagon (no belief↔meme bridge), 4 unit + 4-leg integration tests (incl. end-to-end injected-dominant-narrative run); ZERO-BLAST — golden + 14 snapshots byte-identical, NO regeneration, 830 sim + 216 integration green. **trauma → group psychology WIRED (Iter 121)** — shared-trauma bonding in peer-group formation pressure (`shared_trauma` = mean members' nervous trauma_load, `TRAUMA_BONDING_WEIGHT` 0.2 = grievance weight, serde-default field; formation entry point only — cohesion feedback deferred; probe: trauma high everywhere (0.13–0.93) yet zero groups form — fold live-but-inert, ZERO-BLAST, golden + 14 snapshots byte-identical, 833 sim + 217 integration green). Remaining: `cross_cutting_ties` stored as u32 count vs plan's Fixed tie-ratio; `sacred_events` uses synthetic `EventId`s (revisit if a real event log lands). **Iteration 115**: the §13.5 moral-panic lifecycle is now LIVE — the previously unit-test-only `MoralPanicRegistry`/`MoralPanic` machinery (register/escalation_pressure/escalate/deescalate/should_resolve) is driven daily by `tick_moral_panic_lifecycle` (fatigue resolves the panic at day 35, pressure > 0.5 escalates it), the §7.2 trigger now registers panics, and an ACTIVE panic's intensity erodes its target institution's legitimacy (`panic_legitimacy_drain`, rate 0.001 calibrated — 0.005 was probed to kill the birth pipeline). BIG-blast in panic-frequent worlds: births re-paced [21860, 45710]→[28230, 35960] and the pestilence epidemic settles to an endemic plateau (both intent-preserving recalibrations); golden (1,000) + snapshots (≤2,000) untouched — no panic fires before 3034 | Iter 57/115 | Producer-side only → lifecycle WIRED |
| 15 | §29.2 | **WIRED (Iter 100)** — `suppression_resistance()` blends the armed core with `threat_level` (cohesion/grievance/anti-fragmentation) and amplifies by `legitimacy_of_violence` (radicalization); the protest-suppression force comparison reads the full threat model (never below raw strength, zero-at-zero preserved). `fighting_strength` still drives actual conflicts by design | Iter 74 | Wired |
| 16 | §8.1.10 | **WIRED (Iter 90–93)** — Respect Elders gates hostility toward elder-role holders (Iter 90/91) and Obey Ruler suppresses defiance toward the Guard Captain (Iter 93); `Institution.norm_ids` now consumed (temple ×1.5 reinforcement, Iter 90) | Iter 82–89 | Wired |

**Reserved taxonomy (structural, no behavioral intent yet):** `LordVassal` + `GuardCitizen` stages (no live producer); `KinshipLink::Cousin` type reserved; `InLaw` edges never created by code (stage reachable only if edges exist); legacy `culture::ideology::BeliefEcology` dead struct (deliberately untouched); the §10.4 probabilistic daily ladder roll **superseded by design** (deterministic gates keep replay byte-identical — do not "fix" it).

---

## 2. Testing — Coverage Gaps

| Gap | Detail |
|-----|--------|
| **Behavioral proofs — harness BUILT (Iter 134), scenario-context extension (Iter 138)** | New `run_scenario_with_params` + `scenario_behavioral_delta` allow probing consumers under Drought/Famine/Collapse shocks. Demo: conflict channel live in BOTH calm and drought (drought delta +1435, calm +1704 — thirst dampens conflict). Remaining: applying the harness to additional §1 consumers in their firing windows (longer horizons or non-drought scenarios). |
| **Population scale — RETIRED (Iter 132)** | The designed settlement size is the §19.5.F cap (MAX_POPULATION 48, enforced at populate + every birth gate). New scale_tests.rs validates AT full capacity: populate clamping (request 100/200 → exactly 48), through-run cap-safety sampling (count never exceeds 48 at 100-tick cadence over 600 ticks), all-22-emotion bounds + resource non-negativity at the cap, and full-capacity seed-determinism. Probe-pinned vacuity note: seed-42 village is perfectly healthy (count 48 at every sample to 2000) — the birth gates under churn are covered by the famine/pestilence integration tests. 100s/1000s-agent runs above the cap would require raising MAX_POPULATION (a settlement-design change, queued with §17.4 sparse passes if multi-settlement lands). |
| **Long-horizon determinism — RETIRED (Iter 135)** | New `long_horizon_tests`: seed 42 run twice to 50K is byte-identical in BOTH the metrics snapshot and a per-agent core-state projection (aggregate means can cancel; the projection cannot), plus probe-pinned emergence (factions/panics/memes fired, rituals EXECUTED via `last_occurrence > 0`, population stable 12→13, no collapse); seeds 7/99 to 50K hold every core invariant. Probe finding: `RitualParticipated` memory traces decay below the eviction threshold before 50K — `last_occurrence` is the durable execution signal. Gate +~72s. |
| **Scenario battery — RETIRED (Iter 140)** | 6 scenarios (riverford/drought/famine/pestilence/collapse + CALM). The Calm scenario (no shocks) is the control for scenario-based differential testing, exercised via `conflict_delta_in()` in behavioral_delta.rs. The ShockKind machinery remains extensible for future scenario types. |
| **Statistical emergence — RETIRED (Iter 137)** | D4 reachability: `courtship_d4_reachable_across_worlds` proves the gate is structurally reachable (10 seeds, 5000 ticks, ~33s). Faction variance: `faction_counts_vary_across_worlds` proves non-deterministic emergence (20 seeds, 2000 ticks, ~26s). Rumor saturation: the existing `rumors_transmit_through_population` and `rumor_evidence_degrades_with_transmission_hops` tests (integration_tests.rs) already provide adequate coverage. |
| **Property tests — RETIRED (Iter 139)** | 4 new invariant-style tests: norm strength in [0,1], attachment dimensions in [0,1], moral emotions in [0,1], institution legitimacy in [0,1] — all across 10 seeds at 500 ticks. 21 property tests total. |
| **Snapshot coverage — RETIRED (Iter 136)** | The first >4320-tick snapshot is live: `snapshot_long_horizon_surface_10000_ticks` pins the full norm/memory/attraction surface at 10000 ticks (both ritual firings, 4320/8640, captured — `executed_ritual_count: 2`, `latest_ritual_occurrence: 8640`), now safe because Iter-135 proved 50K byte-identity. 15 insta snapshots total. Probe-pinned honesty: the attraction surface sits at init defaults in the calm seed-42 village (D4 gate rarely opens), and `Semantic` memory absence marks the schooling surface dormant at 10K. |

---

## 3. Validation — Documentation & Process Debt

| Item | Detail |
|------|--------|
| **State-doc test tables — RETIRED (Iter 133)** | §9.1/§9.2/header/section-§1 refreshed to measured reality at commit `5c45f44`: **1,099 tests** (849 sim + 230 tests-crate + 20 core, via `cargo test -- --list`), 0 clippy warnings (strict `-D warnings` gate restored Iter 131), 0 TODO/FIXME, 450 commits, 67,253 lines of Rust, date updated. |
| **Clippy debt — RETIRED (Iter 131)** | The 97-line warning debt is GONE — `cargo clippy --all-targets -- -D warnings` now compiles clean across every crate (sim + tests + core + cli + tui + benches). Sweep: clippy --fix auto-applications + 25 manual field_reassign_with_default initializers + the sim.rs:2940 status-loop enumerate refactor (with the cross-agent kinship read precomputed into `agent_ages`) + sort_by_key/Reverse for the descending sorts + clone_from + the dead `&&` RHS + bench missing_docs. PROVABLY behavior-neutral: 849 sim + 226 integration green, golden byte-identical, 14 snapshots byte-identical. The strict gate is restored. |
| **State doc navigability — RESOLVED (Iter 133)** | The Iterations 22–48 wall-of-text footnote became `## 13. Iteration Changelog` in MINDSTRATA_CURRENT_STATE.md: every iteration 22–132 is now one grep-able `- **Iteration NN**` bullet, sorted newest-first (script-verified continuity, no missing/duplicate numbers). |
| **Golden recalibration queue** | Each decisional consumer in §1 will require a deliberate, documented golden recalibration (Iter-66 precedent: meme mutation). No backlog exists tracking which consumers are slated and in what order. |

---

## 4. Optimization — Performance & Scale

| Item | Detail |
|------|--------|
| **O(n²) daily passes — PARTIALLY RESOLVED (Iter 142)** | The daily rumor pass's n×n trust-matrix allocation + fill is GONE: `transmission_pass_lazy` reads trust directly from the packed `relationship_v2s` via a closure (byte-identical golden, proven by the lazy-vs-matrix equivalence test). Remaining O(rumors × n): the per-rumor argmax listener scan (inherent to the spread model) — cheap at village scale. |
| **Meme aggregation — RESOLVED (Iter 143)** | The §17.4 lazy strategy is live: `wire_meme_aggregation` gates its O(agents×categories) + O(agents×households) input builds behind `should_compute` (the same condition `aggregate()` uses internally), running the expensive pass only on aggregation ticks while the exposure-sample flush stays daily. The plan's sparse/lazy aggregation is now implemented and behavior-neutral (golden byte-identical). |
| **Benches — PARTIALLY RESOLVED (Iter 141)** | Per-system benches SHIPPED: `subsystems.rs` criterion target isolates the memory-encode hot path (fresh 16.7ns / near-capacity 175ns) and the gossip single-hop pass; `tick_loop.rs` extended to the 48-agent cap + a 10K-tick long-horizon run bench. Still missing: release-mode behavioral benchmark (measurements above are debug-vs-release unverified) and per-system regression thresholds wired into CI. |
| **Cognitive budget at scale — PARTIALLY RESOLVED (Iter 144)** | The wired §17 gates are now PROVEN to have teeth: a live-run test forces Background vs Focal agents and pins exactly-frozen memory (Background) vs new-trace encoding (Focal), and the Secondary→Background demotion path + its zero-budget caps are unit-tested. Background-tier behavior is now measured at village scale. FINDING (queued, NOT shipped): `runs_social_interactions()` is dead code — Background agents still do full social interactions; wiring it is a calibrated change. Background behavior at 100s of agents (the tier mix at scale) remains unmeasured (needs the multi-settlement scale work). |
| **Level-of-detail** | Focal-only narrative (`runs_narrative`) is the main tier gate; the §17.1 Focal/Secondary/Background calibration is not validated against a population mix. |

---

## 5. Beyond AP2 — New Game-Level Features (state-doc §12 priorities)

**Phase 2 — Deepen:**
- **Multi-settlement** — neighboring villages, trade routes, diplomacy, conflict
- **Technology tree** — knowledge prerequisites, innovation chains
- **Weather system** — temperature, rainfall, droughts, floods (only scripted shocks exist today)
- **Time-based resource spoilage** — Iter 33 handled storage-overflow spoilage; inventory rot over time remains

**Phase 3 — Add depth:**
- **Legal system** — courts, trials, property rights
- **Education system** — schools (apprenticeships are live, schools are not)
- **Religious mechanics** — theological beliefs, calendar (rituals are live)
- **Military system** — organized warfare, conscription

**Phase 4 — Playability:**
- **Interactive TUI** — the TUI is 999 lines; agent selection / issuing commands is the interactive gap
- **Visual rendering** — 2D map with agent sprites (no frontend exists)
- **Save/load UI** — snapshot management
- **Modding API** — formal interface for custom content

**Explicitly optional by plan:** §9.3 external LLM role (LLM-driven focal agents) — unimplemented, by design.

---

## 6. Recommended Prioritization

**Quick wins (low risk, no recalibration):**
1. ~~Clippy `-D warnings` sweep~~ — DONE (Iter 131): 97 → 0 warning lines, strict gate restored, provably behavior-neutral
2. ~~Refresh state-doc test tables + restructure the iteration log~~ — DONE (Iter 133): §9.1/§9.2 refreshed to measured reality; `## 13. Iteration Changelog` is one grep-able bullet per iteration (22–132, newest first)
3. ~~Add the missing 2 pre-existing-warning fixes (sim.rs:8445, integration_tests.rs:5050)~~ — DONE (Iter 131, part of the 97→0 sweep)
4. ~~Strengthen property tests + build the behavioral-delta harness~~ — DONE (Iter 134): the invariant-style property sweep already existed (17 tests); the reusable `behavioral_delta` same-seed consumer on/off harness is live with 4 validation/demo tests and a documented calm-window inertness sweep

**Behavioral wiring (each recalibrates; highest game-fidelity value):**
5. §7.2.6 conception→pregnancy→birth pipeline (largest inert channel)
6. §8.1.10 Respect Elders / Obey Ruler + `Institution.norm_ids` (temple) — the natural continuation of Iterations 82–89
7. §8.1.4 expanded emotions → decisions; §9.2 neural-like → action selection
8. §10.4 "seek proximity" + attraction → courtship behavior (closes the documented stall)

**Scale & polish:**
9. Large-population validation (100+ agents) + sparse O(n²) passes
10. Multi-settlement + weather (biggest structural additions beyond AP2)

---

*Report generated from `docs/architecture/AP2.md`, `docs/MINDSTRATA_CURRENT_STATE.md`, and live codebase audits at commit `5c45f44`. All test counts and clippy numbers verified against the workspace at report time (Iteration 133 refresh).*
