# Mindstrata — AP2 Audit Findings Log

**Status:** Phase 1 of 14 complete. Generated during Phase 1 execution at commit `d8af39c` (Iteration 181).
**Verdict legend:** FULL · WIRED-LIVE · WRITE-ONLY · PARTIAL · MISSING · SUPERSEDED · DEAD-PARAM · UNVERIFIED (see `docs/architecture/AP2_AUDIT_PLAN.md` §0.2).
**Severity:** S1 plan-mandate absent/dead · S2 plan effect does not occur · S3 live but under-probed · S4 cosmetic/spec drift.

---

## Phase 1 — Baseline Integrity & Determinism Core

**Date:** August 14, 2026 · **Commit:** d8af39c (tree clean) · **AP2 §:** 4.1, 6, 14.1, 16.1

### Verdicts

| System | AP2 § | Verdict | Evidence (probe/test + numbers) | Severity | Action |
|---|---|---|---|---|---|
| Multi-timescale scheduler | §6 | `FULL` | 10 phases verified with exact intervals (Fast 1 / Hourly 6 / Deca 10 / Duodeca 12 / Centum 100 / Daily 144 / Quincent 500 / Weekly 1008 / Seasonal 4320 / Yearly 51840); `TickPhases::compute` called exactly once per tick (sim.rs:2587); 7 scheduler unit tests pass (interval, boundary, tick-0, phase-hierarchy) | — | — |
| Determinism core | §4.1 | `FULL` | `golden_replay_vs_baseline` byte-identical (0.77s); `long_horizon_50k_is_deterministic_and_emerges` + `long_horizon_50k_multi_seed_invariants` pass on seeds 42/7/99 (70.88s); 21 property invariants pass (bounds, no negative age, belief∈0..1, no resource duplication) | — | — |
| Snapshot round-trip | §16.1 | `FULL` | Manual 10K-class proof: continuous[0..2000] vs save@2000→load→resume[0..2000] **byte-identical across all 34 CSV columns** (tick-2000 row matches exactly); `save_load_round_trip` CLI test ok; 9 snapshot sim units pass (incl. JSON round-trip) | — | — |
| Compatibility facade | §14.1 | `FULL` | Legacy blocks (needs/body/actions) intact and reading `derived_*` — proven by golden byte-identity + 1,314-test suite listing clean | — | — |
| RNG stream isolation | §4.1 | `FULL` (1 S4) | 7 streams; every draw site uses a domain-matching stream (World=world_gen, Behavior=shocks/movement/utility, Social=interactions/detection, Economy=market, Ecology=season, Narrative=technology/meme). Zero cross-stream contamination found | S4-1 | see below |
| from_snapshot restore paths | §16.1 | `FULL` | All restore paths present + documented: weather fresh (reseed comment), technology reconcile_discovered, legal/diplomacy/school/theology/military fresh, meme-registry v9 guard + `is_empty()` re-seed (no double-seed), rituals/campaigns/clans/collective-memory/noosphere re-seed, group_registry `rebuild_cache`, kinship v10 | — | — |
| Fixed/clamp hygiene | §4.1 | `FULL` (1 S4) | Iter-112 amplifier confirmed: `collective_fear_panic_amplifier` in relational_field.rs, ONE-SIDED identity below 0.95 anchor, cap unit test `_is_identity_below_anchor_and_capped`; Iter-172 tone floor confirmed: `STRESS_RECOVERY_TONE_FLOOR_FIXED` hoisted Fixed const, applied via `max(tone, floor)`; drought well-water no-clamp comment verified (legitimate >1.0 quantity) | S4-3 | see below |
| TUI operational | §5 (Phase 5) | `FULL` (1 S4) | tmux live run, seed 42 / 6 agents: `n` tick 0→1→36, `j`×2 selection 0→2, `t` full 5-view cycle, Space pause→run→pause (auto-advance 1→35), `w` queued Work directive for Cara, `x` cleared it, `q` clean exit. **Zero panics/errors in full scrollback** | S4-4 | see below |
| Release tick throughput | §17 (feeds Phase 10) | `FULL` | `tick_probe --release`: 5,977 ticks/s @12 agents; 2,017 @24 agents/2000t; 2,178 @24 agents/5000t — comfortably above the CI regression-gate floor (600/s) | — | baseline for Phase 10 |

### S4 observations (no behavioral impact; logged for the cleanup queue)

- **S4-1 — `RngStream::Psychology` is 100% dead.** Zero draw sites in the entire codebase (only rng.rs references it: definition + `get_mut` arm). All psychology-domain randomness flows through sim.rs using `Behavior`/`Social` streams. Harmless (deterministic, no contamination) but it is a seeded-and-allocated stream with no consumer. **Options:** (a) wire psychology draws into it for stream purity, or (b) delete the variant + `ALL` entry (verify no serializer depends on the enum). Candidate for a dedicated zero-blast iteration.
- **S4-2 — Module-local cadences still use raw `is_multiple_of`.** `sim.rs:3617/3629/3648/3707` (144), `sim.rs:5081` (4320), `diplomacy.rs` `DIPLOMACY_CADENCE` (4320), `factions.rs:191` protest cooldown, `meme_aggregator.rs:131` aggregation interval, `relationship_v2.rs:600` (144). All intervals match the phase system, so gating is consistent — this is a consolidation opportunity (route through `TickPhases`), not a bypass. Zero behavioral impact.
- **S4-3 — `endocrine.rs:79` uses `Fixed::from_f64(0.1)` in the per-tick `StressAxis::update`.** 0.1 = `Fixed::from_raw(1000)` exactly at the 4-decimal scale; hoisting to a const would match the Iter-172/176 hoisting precedent. Cosmetic perf nit at village scale.
- **S4-4 — TUI status-footer messages are clipped at 120 columns** (Paragraph without wrap clips rather than wraps): `w`/`x` feedback was invisible at default pane width and required `tmux resize-window -x 180`. Cosmetic; verify-command feedback should wrap or the pane should be widened.

### Confirmed-in-place (no action; prior iterations verified intact)

- Iter-112 collective-fear panic amplifier (capped ∈ [1.0, 1.5], identity at/below 0.95 anchor above calibrated peak 0.903).
- Iter-172 `STRESS_RECOVERY_TONE_FLOOR` (0.3 floor, stress mean 0.42–0.58 documented equilibrium).
- Iter-181 narrative scripts bounded (probe re-run this phase: seed42/10000 red=0.539 cont=0.312 hero=0.812 — no saturation; resilience factor mean 0.990, focal-only containment `focal_off=12` confirmed).

### Acceptance criteria (Phase 1)

- [x] Golden replay byte-identical
- [x] 50K determinism holds (seeds 42/7/99, metrics + per-agent projection)
- [x] Phase map complete: 10 phases, exact intervals, compute-once-per-tick
- [x] Zero cross-stream RNG draws (1 dead stream noted S4-1, no contamination)
- [x] Snapshot resume byte-identical (manual 34-column CSV diff + CLI test)

### Residuals → iteration queue

| Residual | Severity | Suggested iteration |
|---|---|---|
| S4-1 dead `RngStream::Psychology` (wire or delete) | S4 | zero-blast cleanup iteration |
| S4-2 raw `is_multiple_of` consolidation | S4 | cosmetic refactor iteration |
| S4-3 `endocrine.rs` from_f64(0.1) hoist | S4 | cosmetic perf iteration |
| S4-4 TUI footer clipping | S4 | TUI polish iteration |

---

## Phase 2 — Biological Substrate (§7)

**Date:** August 14, 2026 · **Commit:** d8af39c · **AP2 §:** 7.2.1–7.2.9, 7.3, 7.4

**Instrument:** new probe `crates/mindstrata-benches/examples/biology_probe.rs` (16 runs: seeds {42,1,7} × {calm,drought,famine,pestilence} @5000 + seeds {42,46} × {calm,famine} @20K). All 66 biology unit tests + 8 snapshot tests + 12 behavioral-delta tests green. Golden byte-identical (probe is read-only).

### Verdicts

| System | AP2 § | Verdict | Evidence (probe/test + numbers) | Severity | Action |
|---|---|---|---|---|---|
| Genome | §7.2.1 | `FULL` (struct present, predispositions consumed via psychopathology/attachment/derived-health) | deterministic random + express_trait unit-tested; child genome inheritance integration-tested (stress_reactivity child ∈ [0,1], mutation differs from parent) | — | — |
| Endocrine — stress axis | §7.2.2 | `FULL` (Iter-172 fix holds; documented bimodal residual) | probe calm: mean 0.417, 7/12 at 0.0 + 5/12 at 1.0 — the DOCUMENTED chronic-load-pinned sub-population; pestilence mean 0.667 (directional ↑) | S3 (documented) | residual is the known Iter-172 chronic-load pacing bottleneck |
| Endocrine — bonding/dominance/growth axes | §7.2.2 | `FIXED` (S2-2-2; probe-verified live) | `bonding.update` wired into the ritual duodeca loop (input `bonding_effect × 0.5`, recovery `endocrine_bonding_recovery` 0.02) with **saturating (1 − level) gain** — bounded at a stable equilibrium ≈0.78 max by construction (reviewer-caught: the linear form ratcheted to 1.0 at ~30K ticks; regression test pins 11 fires ≈ 50K window); `dominance.update` wired into the daily status pass (mean-reverts toward `effective_status` × `endocrine_dominance_response` 0.1); `GrowthAxis::update` added (was absent — the axis had no update method at all) + wired into `EmbodiedState::tick_update` toward `LifeStage::growth_modifier()` × new `endocrine_growth_recovery` 0.001. Probe after (5K/20K): bonding mean 0.42–0.50 max 0.647 (differentiated, no saturation at any horizon), dominance 0.18–0.48 (tracks standing), growth 0.40–0.70 (age-differentiated) | — | 4 new endocrine unit tests (incl. 50K saturating regression); re-pinned `endocrine_states_500_ticks` snapshot; golden byte-identical |
| Endocrine dead accessors | §7.2.2 | `WRITE-ONLY` | `stress_cognitive_modifier()`/`bonding_trust_modifier()`/`dominance_aggression_modifier()` — zero callers (stress.level consumed directly elsewhere, so the modifier layer is dead weight) | S4 | delete or wire |
| Nervous — sympathetic/parasympathetic | §7.2.5 | `SATURATED` | probe: sympathetic 9–11/12 agents at 1.0 in EVERY scenario INCLUDING calm (parasympathetic drained to 0 for 9–11/12) — the Iter-172 stress-axis saturation pattern still live in the nervous channel; derived accessors `effective_arousal`/`attention_narrowing`/`social_engagement_capacity` have zero callers (startle already flagged write-only in behavioral_delta.rs:329) | S2 | add recovery floor / rebalance rise-vs-recovery; wire or delete accessors |
| Nervous — pain | §7.2.5 | `WIRED-LIVE` (inert: no injuries in probed windows) | pain 0 everywhere (calm/famine/pestilence); consumed in `derived_health` (×0.1) + endocrine acute_stress — correct behavior, needs a conflict-world probe to show liveness | S3 | conflict-world probe (Phase 6/13 will cover) |
| Nervous — trauma | §7.2.5 | `FULL` (Iter-176 fix verified live) | probe: trauma mean 0.38–0.45, max 0.50, DIFFERENTIATED (no saturation) across all 16 runs; 3 unit + 1 behavioral-delta test green | — | — |
| Skeletal | §7.2.3 | `WIRED-LIVE` (neutral-at-baseline by design) | integrity 1.0 / mobility 0 in all probed windows (ages 20–54, no elders); unit tests pin elder-frailty + injury + malnutrition paths; `effective_mobility` consumed (sim.rs:10909) | — | elder/frailty worlds are out of calibrated horizon — acceptable per design |
| Muscular | §7.2.4 | `PARTIAL` | conditioning STATIC 0.400 in every scenario — the gain band (0.3 < activity < 0.7) excludes Work (0.8); `effective_strength()`/`exhausted()` zero callers | S2 | widen the conditioning band to include Work; wire strength into labor output |
| Cardiovascular | §7.2.9 | `PARTIAL` | fitness STATIC 0.500 in every scenario — gain band (0.3 < activity < 0.8) excludes Work (0.8, strict `<`); `collapse_risk()` zero callers | S2 | widen fitness band / treat Work as 0.75; wire stamina into actions |
| Respiratory | §7.2.8 | `FULL` (lung_health differentiates) | probe lung_health 0.30–0.80 mean 0.72 (age-driven decline visible); `effective_capacity()` zero callers (S4) | — | — |
| Immune | §7.3.1 | `PARTIAL` | resistance ratchets to 1.0 for all (nutrition 0.002 + sleep 0.001/tick >> stress suppression 0.001×stress — no decay); infection_load 0 in all windows (crowding placeholder 0.3 < 0.5 threshold) | S2 | balance boost vs suppression; wire crowding/injury exposure |
| Digestive | §7.2.7 | `WIRED-LIVE` (neutral-at-baseline by design) | gut_health 1.0, satiety 0.18–0.43 (differentiated); `effective_digestion` consumed in derived_energy; spoiled-food path unit-tested | — | — |
| Thermal | §7.3.3 | `PARTIAL` | body_temp 0.5 / cold/heat stress 0 in ALL runs — `ambient_temperature` HARDCODED 0.5 at sim.rs:2855, so the Iter-147 WeatherTracker's temperature never reaches the biology layer | S3 | wire weather temperature into `tick_update` |
| Reproductive + pregnancy | §7.2.6 | `FULL` | sim.rs:7895–8010 proves conception→pregnancy→gestation→`complete_pregnancy`→birth IS wired (Iter 92); 9 unit + pregnancy lifecycle round-trip tests green; 100K-horizon integration tests pinned in changelog (too slow for debug re-run) | — | see S4-2-1 doc drift |
| Development | §7.2.6/8.1.13 | `FULL` | life-stage ladder unit-tested; `physical_modifier` consumed (age_modifier in cardio/resp/muscular); probe ages 20–54 adult/mature spread | — | — |
| EmbodiedState facade | §7.4 | `PARTIAL` | only derived_health/energy/fatigue consumed (sim.rs:2871–2873); `derived_hunger()` ZERO callers; embodied.hunger/thirst never synced (separate needs track, documented sim.rs:2870) — plan §7.4 "NeedState derived from biology" facade is partial; derived_thirst/sickness/injury/fertility absent | S2 | decide: derive needs from biology or drop the dead facade methods |

### S2/S3/S4 findings → iteration queue

- **S2-2-1 — sympathetic/parasympathetic saturation: FIXED (August 14, 2026).** The nervous channel pinned at 1.0/0.0 for the majority in every window including calm (probe: 9-11/12 agents sympathetic 1.0, parasympathetic 0.0 in lockstep). Three-part fix in `nervous.rs`: (1) **saturating sympathetic rise** — `threat × 0.2 × (1 − arousal)` (the linear form `threat × 0.2` vs floored recovery 0.03 had net +0.03..0.07/tick at the calibrated fear equilibrium, unbounded by any linear calibration); (2) **logistic tone drain** — `drain_coeff × tone` with `drain_coeff = arousal×0.05 + threat×0.05` (the old pure-accumulator form banged tone to 0/1 with no stable middle — 8-11/12 at 0.0 in calm); (3) **recovery ceiling** — `NERVOUS_TONE_RECOVERY_CEILING` 0.6, symmetric to the Iter-176 0.3 floor, bounding the tone→recovery→arousal→drain positive feedback that would otherwise ratchet tone to 1.0 and drain arousal to 0 (unit-pinned). Post-fix probe: sympathetic max 0.85 (zero agents pinned), parasympathetic means 0.13–0.59 with no majority at either extreme, calm settles rest/digest, famine fight/flight. 2 nervous regression tests added (`sympathetic_arousal_does_not_pin_under_sustained_threat`, `parasympathetic_tone_drains_under_threat`). Golden byte-identical; 4 snapshots re-pinned (metrics 500/2000, endocrine 500, long-horizon 10000 — avg_health up as fitness/conditioning now build health); seed-46 birth pin re-pinned 6→3 births (Iter-182 note; 3-chain intact).
- **S2-2-2 — endocrine bonding/dominance/growth never tick: FIXED (August 14, 2026).** The plan's "ritual increases bonding" / "status victory raises dominance" / growth-axis development effects now occur. Wiring: ritual duodeca loop → `bonding.update` (×0.5 input scale; axis uses **saturating (1 − level) gain** — review-caught ratchet fixed: linear form pinned max-receptivity agents at 1.0 by ~30K ticks of monthly fires, the saturating form bounds the axis at equilibrium ≈0.78, regression-tested at the 50K horizon); daily status pass → `dominance.update` (mean-reversion to `effective_status`, replacing the old hardcoded-−0.01 form that had zero callers); `GrowthAxis::update` added + driven from `LifeStage::growth_modifier()` per tick. Knobs `endocrine_bonding_recovery` (0.02) and `endocrine_dominance_response` (0.1) — dead since placement — are now LIVE; new `endocrine_growth_recovery` (0.001) added. Golden byte-identical (axes feed no golden-hashed metric; ritual fires only at 4320 > golden 1000 window). Endocrine 500-tick snapshot re-pinned (dominance now drifts, growth now moves from birth 0.6).
- **S2-2-3 — Work activity never builds fitness/conditioning: FIXED (August 14, 2026).** The gain bands excluded Work (fitness strict `< 0.8`; conditioning `< 0.7`) so the plan's "labor improves fitness/conditioning" was dead for the primary action (probe: fitness frozen 0.5, conditioning frozen 0.4 in every scenario). Bands widened to `<= 0.8` in `cardiovascular.rs` / `musculoskeletal.rs`, gains made **saturating** (`× (1 − axis)`) with **excess-proportional detrain** (zero at the floor, growing with surplus — "use it or lose it"), and the whole computation moved to **f64** (the Iter-176 trap: at 4-decimal Fixed the gain `0.8×0.6×0.0003×0.6 = 0.0000864` truncates to raw 0, silently re-freezing the axis; cardio gain constant 0.0015 lifts the equilibrium off the 0.0001 quantization plateau — the 0.0005 form pinched at exactly 0.55). Post-fix probe: fitness 0.55–0.65, conditioning 0.45–0.55, differentiated across scenarios/seeds, no saturation at any horizon. 4 unit tests added (Work builds fitness/conditioning, both detrain, both equilibrium < 1.0).
- **S2-2-4 — immune resistance saturates at 1.0: FIXED (August 14, 2026).** Root cause was twofold, both now corrected in immune.rs: (1) the linear boosts (nutrition +0.002, sleep +0.001) dwarfed stress suppression (−0.001) with NO decay term, ratcheting resistance to 1.000 in every window including famine — the boosts are now *saturating* (`× (1 − resistance)`, the project's standard logistic pattern), giving a stress-dependent equilibrium `R* = 1 − (stress·0.001 + age·0.0001)/(nutrition·0.002 + sleep·0.001)`: post-fix probe resistance mean **0.764 calm / 0.525 famine** (was 1.000 everywhere), 0/12 pinned, spread 0.333–0.917. (2) The exposure gate (`crowding > 0.5 && hygiene < 0.3`) was UNREACHABLE — the sim wires crowding 0.3 / hygiene 0.6 placeholders, so `(crowding − 0.5)` was negative forever and infection_load never left 0 in any scenario (the entire infection→inflammation→fight path was dead state). Replaced with a **stress-driven exposure channel**: fires only above stress 0.4, `(stress − 0.4) × (1 − hygiene) × (0.5 + crowding) × 0.002`/tick — calm majority (stress ≈ 0.26) stays at zero exposure (golden-safe; 1000-tick golden window never crosses 0.4), while the documented chronic-stress sub-population opens low-grade infection. The fight term is now proportional to infection load (`R·C·0.005·I` — immune response ∝ pathogen load; the old flat fight dominated any per-tick exposure), yielding a self-limiting chronic-infection equilibrium. Post-fix probe: infection_load **calm mean 0.107 (0:8-9/12), famine mean 0.286 (0:4/12), famine-20K mean 0.375 (0:3/12) — zero pinned agents in any window** (was 0.000 everywhere). 4 snapshots re-pinned (avg_health −0.007→−0.033 as chronically-stressed agents now carry low-grade infection; golden byte-identical; seed-46 160K birth pin held at [20950, 91520, 97160]). Note: the proportional-fight change also shifted the wound-infection path's equilibrium from ~0 (old flat fight dominated any per-tick wound exposure) to ~0.46 for a severe open wound (injury 1.0) — injury stays ~0 in calibrated windows, so no pinned behavior changes, but the wound path is now a genuine self-limiting infection.
- **S2-2-4b — endocrine arousal inherits stress saturation: FIXED (August 14, 2026).** `arousal.level` pinned 8-9/12 agents at 1.0 in famine/pestilence (mean 0.78-0.81) — linear rise `stress × 0.3` vs decay `(level−baseline) × 0.1` was net-positive at every calibrated stress. The rise is now *saturating* (`× (1 − level)`, the same logistic pattern as the S2-2-1/S2-2-2/S2-2-4 fixes), bounding the equilibrium at `L* = (s·r + b·d)/(s·r + d)` ≈ 0.61 calm / 0.77 famine. Post-fix probe: **calm mean 0.44–0.49, famine/pestilence mean 0.66–0.68, max 0.85 — 0/12 pinned at 1.0** (was 8–9/12), directionally distinct and bounded.
- **S2-2-5 — derived_fatigue ratchets to 1.0:** `base + positive terms` with no decay → all agents at 1.0 by ~5K ticks (probe: 12/12 in every scenario). The facade "fatigue" is saturated; needs.fatigue (separate track) is the live one. Add a recovery term or drop the facade field.
- **S3-2-1 — weather→thermal not wired: FIXED (August 14, 2026).** The biology pass's ambient input was a hardcoded 0.5 ("temperate default") at sim.rs:2854, freezing the thermal system at thermoneutral in every scenario (probe: body_temp 0.500, cold/heat stress 0, no spread — the Iter-147 WeatherTracker was live but never reached the body). The input is now `self.weather.temperature` (0..1, seasonal Spring 0.6 / Summer 0.9 / Autumn 0.5 / Winter 0.2, mean-reverting + seeded noise). Post-fix probe: body_temp 0.554–0.590 differentiated across scenarios, cold_stress 0.15–0.19 in colder windows, heat stress live — the plan's §7.3.3 "winter hardship" is now real (integration test re-based from the frozen thermoneutral contract: Spring body tracks ≈0.6 ambient with zero cold stress; a Winter leg (ambient 0.2) produces cold_stress > 0.05 and body < 0.48; seed-deterministic). Golden byte-identical and all 8 snapshots UNCHANGED (nothing golden-hashed reads thermal; the respiratory consumer's cold_stress ≈ 0 in Spring/Summer/Autumn); the seed-46 160K birth pin held at [20950, 91520, 97160]. The biology pass reads the previous tick's weather state (block order), matching the one-tick lag the weather site documents — deterministic, RNG-free.
- **S3-2-2 — pain liveness unproven in conflict worlds:** pain 0 in all probed scenarios; needs a conflict/combat-world probe (Phase 6).
- **S4-2-1 — reproductive.rs doc drift:** module header still says "the biological pregnancy lifecycle is currently dormant" — FALSE since Iter 92 wired should_birth→pregnancy→birth (sim.rs:7895–8010). Update the doc comment.
- **S4-2-2 — dead biology accessors:** `effective_strength`, `exhausted`, `collapse_risk`, `in_crisis`, `effective_capacity`, `effective_arousal`, `attention_narrowing`, `social_engagement_capacity`, `sleep_deprived`, `should_sleep`, `derived_hunger`, 3 endocrine modifiers — zero callers. Delete or wire.

### Acceptance criteria (Phase 2 — re-audited August 14, 2026, post S2-2-1/2/3 + S3-2-1 fixes)

- [x] All §7 systems exist, wired into `EmbodiedState::tick_update`, 78 biology units green
- [x] Snapshot differentiation verified (endocrine/agent snapshots pass, 8/8; golden byte-identical)
- [x] Trauma differentiated (0.24–0.43 across scenarios), stress FIXED in calm (0.25–0.26 mean, was saturated); the stress residual is now confined to famine/pestilence worlds (0.667, 8/12 at 1.0) — arguably correct directionality, but see S2-2-4-family note below
- [x] Pestilence directional collapse verified (energy 0.500 ↓ from calm 0.833, stress 0.667 ↑, trauma 0.43 ↑ — the energy value moved from 0.333 to 0.500 after the S2-2-3 fitness/conditioning fix; direction intact)
- [x] Thermal LIVE (S3-2-1 fix): body_temp 0.554–0.590, cold_stress 0.15–0.19 once the 20K window crosses into Winter; heat stress live
- [x] Endocrine axes decisional: bonding 0.33–0.53, dominance 0.20–0.45, growth 0.50–0.70, fitness 0.65, conditioning 0.48–0.55 — all differentiated, no saturation
- [x] Nervous channel de-saturated: sympathetic max 0.85 (was 9-11/12 at 1.0), parasympathetic 0.13–0.59 no majority at either extreme
- [ ] **FAILED (residual):** facade `fatigue` (S2-2-5) still pinned at 1.0 for 12/12 in every window while the real muscular.fatigue track is healthy (0.49–0.60) — the facade field is a write-only ratchet (`base + positive terms`, no decay); the stress-axis residual remains confined to famine/pestilence (0.667, 8/12 at 1.0 — the Iter-172 chronic-load sub-population; now the only remaining stress saturation); hunger/thirst facade flat 0.20/0.10 in every scenario (separate needs track, documented)
- [x] **RESOLVED:** every §7 endocrine axis decisional — bonding/dominance/growth wired live (S2-2-2 fix); fitness/conditioning dead for Work fixed (S2-2-3 fix); sympathetic/parasympathetic no longer majority-pinned (S2-2-1 fix); weather→thermal wired (S3-2-1 fix); immune resistance de-saturated + infection path made REACHABLE and stress-directional (S2-2-4 fix — 0.764 calm / 0.525 famine resistance, infection 0.107 calm / 0.286 famine / 0.375 famine-20K, zero pinned); endocrine arousal de-saturated (S2-2-4b fix — 0.44–0.49 calm / 0.66–0.68 famine, max 0.85, zero pinned)
- [x] Neutral-by-design axes confirmed (NOT bugs): skeletal integrity, digestive gut_health, blood_volume sit at 1.0/0.0 exactly as the "exactly neutral at baseline" contracts specify — the probe's SATURATED-HIGH flag on these is a false positive for by-design invariants (they move only under elder frailty / spoiled food / severe injury, none present in calibrated windows); `age` SATURATED-HIGH is a probe artifact (age is in years, not 0–1)

## Phase 3 — Psychological Runtime (§8)

**Method (August 14, 2026):** read all 17 psychology modules + the sim wiring (appraisal pipeline, emotion decay, D1–D6 prospection ladder, narrative fold, self-model update, regulation strategy selection, skill call sites, LOD gates); built `crates/mindstrata-benches/examples/psych_probe.rs` (12 runs: {calm, famine, pestilence} × seeds {42, 7} × horizons {2000, 10000}, ~50 axes + 22-emotion liveness ledger + categorical distributions; **artifact kept in-repo** like biology_probe). Traced the full production emotion pipeline mechanically: `mem::take` preserves previous-tick values into the parallel array → appraisal delta added → proportional decay (`BASE 0.06` / `SECONDARY 0.12`) → contagion/envy daily adds → write-back. Consequence: **per-tick emotion ≈ delta + (1 − rate)·prev, equilibrium = delta/rate (clamped)** — the key that explains every saturated value below.

### Verdicts

| System | § | Verdict | Evidence |
|---|---|---|---|
| Appraisal emotion pipeline | §8.1.4 | `FULL` | 22/22 producers wired (appraisal.rs + sim.rs:4564–4665); 14 live in calibrated windows (fear, anger, joy, shame, awe, gratitude, loneliness, tenderness, relief, hope, nostalgia + **guilt/pride/trust — P2/P3 closure**), 8 input-unreachable with documented inert ONE-SIDED reasons (sadness/despair/disgust/contempt/humiliation/jealousy/envy/moral_outrage) |
| Attachment | §8.1.14 | `FULL` | security 0.727 (0.51–1.0), anxiety 0.194, avoidance 0.215 — live, differentiated, stress-directional (pestilence anxiety 0.305) |
| Psychopathology | §8.1.15 | `FULL` | depression/anxiety/ptsd/paranoia/overall_health all live with per-agent spread; pestilence depression 0.236 ↑, anxiety 0.305 ↑ |
| Interoception | §8.1.2 | `LIVE` | negative_bias/sensitivity live, differentiated |
| Motivation | §8.1.5 | `LIVE` | needs live (thirst 0.23–0.35, fatigue 0.30–0.42); hunger now crosses 0.5 in famine (P3-6 CLOSED — the famine genuinely starves) |
| Cultural cognition | §8.1.18 | `WIRED-LIVE` | conservatism/max_taboo live; 7 taboos/agent; taboo_penalty consumer wired |
| Theory of Mind | §8.1.9 | `LIVE` | mind models 0–9/agent, develops over horizon |
| Decision policy | §8.1.20 | `LIVE` | decision_count grows 297→1755 over horizons |
| Cognitive runtime / EF | §8.1.12 | `LIVE` | effective_capacity/inhibition/impulsivity differentiated; D5 EF gate wired |
| Temperament + trait plasticity | §8.1.6 | `WIRED` | observational layer live (Iter-162 channels fixed); core traits yearly-gated (Iter-179); altruism→help consumer (Iter-180) present |
| Narrative identity | §8.1.17 | `FULL` | redemption/contamination/heroism/coherence live + Iter-181 decay; victimhood/chosenness/shame now REACHABLE (P3-9 CLOSED — pestilence victimhood 0.340, shame 0.188, chosenness 0.183–0.364; see closure note) |
| Self-model | §8.1.7 | `LIVE` | self_esteem live (0.539–0.607); coherence 0.641 (was 0.600 frozen) + security 0.79–0.997 (was 0.500 frozen) now produced — P3-2 CLOSED |
| Prospection | §8.1.16 | `LIVE` | dread 0.39 / optimism 0.45 / catastrophic 0.43 / planning 0.34 live; hope now produced via the D6 hopeful scenario — live 0.127 at 2K with spread, 0 at 10K only under the D1 acute-scarcity gate (real world depletion) — P3-3 CLOSED |
| Emotion regulation | §8.1.8 | `LIVE` | capacity live & stress-directional; strategy now personality-diverse (Humor 6 / Reappraisal 2 / SeekingSupport 3 / Suppression 1 in calm@10K — was 100% Reappraisal); current_effort now load-scaled (0.49–0.53, was write-only 0.3) — P3-4 CLOSED |
| Moral cognition | §8.1.10 | `FULL` | outrage 0.678 live; moral_emotions shame 0.351 / pride 0.008 live in stress windows; norms internalize DIFFERENTIATED (P3-11 CLOSED — norm_count 3.667, min 1, max 5 at 10K) |
| Skill system | §8.1.19 | `LIVE` | practice wired into the §4.2 skill pass (sim.rs:11451), form_habit + execute_habit wired into action selection; skill_count 2.4–3.7 & habit_count 0–1.2 (was 0.000) — P3-1 CLOSED |
| Imagination / mental scenarios | §8.1.13 | `WIRED` | D1–D6 ladder present (scenario_count 3.7–5); hope dead via D2 gate (P3-3) |

### P3 findings → iteration queue

- **P3-1 — §8.1.19 SkillState is structurally unwired (the largest dead system in §8).** `SkillState::practice`, `form_habit`, and `execute_habit` have **zero production call sites** in the entire codebase (only unit tests). The sim calls only `decay_habits` + `update_automaticity` (sim.rs:3439–3442) on an always-empty set. Probe: `skill.skill_count` 0.000 and `habit_count` 0.000 for 12/12 agents in every window (the plan's "agents learn skills through practice and form habits" never happens). `automaticity` is "live" (0.147–0.207) but its habit-strength term is always 0 — it reads only the stress boost. Note: the *person-level* §4.2 action skills (sim.rs:11444) are a separate, live system; this finding is specific to the psychology SkillState. **CLOSED (Iteration 183):** `practice` now fires from the §4.2 skill pass when an agent's current action matches a tracked skill, `form_habit` fires on 0.1-proficiency milestone crossings, and `execute_habit` is a fallback in action selection when a triggered habit matches the current motive. `skills` was converted `HashMap → BTreeMap` to preserve byte-deterministic serialization (the snapshot round-trip hash test caught the non-deterministic iteration order). Probe post-fix: skill_count 2.4–3.7, habit_count 0–1.2 across windows (was 0.000).
- **P3-2 — SelfModel.coherence & security are write-only dead fields.** No production writer touches `coherence` (probe: 0.600, 12/12 identical) — it is only *read* (plasticity identity_integration, sim.rs:4895). `security` is only reduced by `threaten_identity()`, which has **no production call site** (only a unit test; the §8.1.7 identity-threat consumer is unwired) — probe: 0.500, 12/12 identical. Both are consumed-but-never-produced state. **CLOSED (Iteration 183):** `update_coherence` (identity-integration-driven) and `update_security` (self-esteem/social-support-driven) added and wired after `reconcile_self_esteem` in the self-model block. Probe post-fix: coherence 0.641, security 0.79–0.997, both differentiated.
- **P3-3 — prospection.hope is dead in every calibrated window (0.000, 12/12).** The D1–D6 ladder's positive scenarios (D4 courtship / D5 hopeful default) are starved because the **D2 threat gate (`fear > 0.4`) fires for the majority** — the calibrated ambient fear is 0.65+ (contagion-maintained), so most agents generate negative threat scenarios daily; `hope` (weighted positive valence) collapses to 0 while `dread` stays live at 0.39. The plan's hope/dread dyad is one-sided in real runs. `update()`'s only hope operation is a depression drain — no independent production. **CLOSED (Iteration 183):** added a D6 hopeful-default scenario to the ladder (fires when the threat/scarcity gates don't) so every agent produces a positive valence trace. Probe post-fix: hope live at 2K (0.127, spread 0–0.5); at 10K it returns to 0 because the D1 acute-scarcity gate (world food/water genuinely depleted in the 16×16 world by 10K) fires daily — a real world-balance observation, not dead wiring.
- **P3-4 — emotion-regulation strategy selection is single-strategy (100% Reappraisal in every window).** `preferred` is never assigned after construction (no production `preferred =` site), and `select_strategy`'s first branch (`stress > 0.3 && reappraisal_skill > 0.4`) catches every agent at the calibrated ambient stress — the extraversion→Humor branch is unreachable. `current_effort` is a per-tick set-constant (0.3, written by `apply_strategy` after `update_capacity` decays it) — no differentiation, no consumer differentiation. The plan's §8.1.8 culture/personality-driven strategy diversity is unwired. **CLOSED (Iteration 183):** `initialize_from_personality` maps extraversion→Humor, agreeableness→Reappraisal/SeekingSupport, neuroticism→Suppression at construction, and `current_effort` is now set from the emotion load (`fear+anger+anxiety`, clamped) at strategy selection. Probe post-fix: Humor 6 / Reappraisal 2 / SeekingSupport 3 / Suppression 1 (calm@10K), effort 0.49–0.53 differentiated.
- **P3-5 — positive secondary-emotion family saturates identically: gratitude 0.880 and tenderness 1.000, 12/12 agents, EVERY window (no spread).** Mechanism (from the equilibrium law delta/rate): the production builder sets `goal_congruence = +0.3` whenever `hunger < 0.5 && threat ≤ 0.2` — true for **all agents in all scenarios** — so `positive = 0.3` is a per-agent *constant*; delta.gratitude 0.15 → eq 0.15/0.12 = 1.25 → clamped 1.0 → ×(1−0.12) = **0.880**; delta.tenderness 0.3 → eq 2.5 → clamped, and tenderness is decay-exempt (Iter-99 consumer calibrated on the saturated state) → **1.000**. Relief/hope/nostalgia (0.79–0.86 means) differentiate only via coping/controllability/coherence. The Iter-116 decay fixed awe (small delta ~0.07 → live 0.49–0.55) but not the constant-positive producers — and the *root cause* is the constant congruence term, not the decay rate. **CLOSED (Iteration 183):** the production goal-congruence term is now per-agent differentiated (scaled by dispositional optimism + current mood) instead of the constant +0.3, and tenderness was added to the secondary-emotion decay (the Iter-99 exemption anticipated "a separate, larger calibration" — delivered here). Probe post-fix: gratitude 0.73–0.81, tenderness 0.55–0.88, both differentiated off-ceiling (was 0.880 / 1.000 uniform).
- **P3-6 — famine/pestilence never raise body hunger (0.006–0.041 in every scenario; famine ≈ calm).** New `needs.hunger/thirst/fatigue` probe axes show body hunger never crosses the 0.5 goal-congruence gate in ANY probed window — the entire goal-incongruence branch (sadness → despair → depression-from-deprivation) is unreachable in all calibrated scenarios, and the famine scenario's psychology is nearly identical to calm (fear 0.653→0.775, loneliness 0.667→0.795 are the only real moves). Same class as the Phase-2 pestilence infection-gate finding: a scenario/wiring gap rather than broken appraisal math. (Thirst IS live and drives the joy channel: `joy = max(hunger, thirst) × 0.3` — the 0.24–0.42 joy means are thirst-driven.) **CLOSED (P2/P3 re-audit):** see the closure note — routine-Eat clock-gating, full-portion consumption gates (Eat/theft/Trade), and a per-tick famine grain drain now starve the village (grain → 0, hunger crosses 0.5, sadness fires).
- **P3-7 — Phase-3 directional acceptance (stress worlds show fear UP / self-esteem & hope DOWN) partially FAILS.** fear ↑ 0.653→0.863 ✓, reg.capacity ↓ 0.415→0.262 ✓, pestilence depression ↑ 0.040→0.236 ✓, pestilence joy ↓ 0.409→0.243 ✓, pestilence hope ↓ 0.853→0.792 ✓ — but **famine moves almost nothing** (hope 0.853→0.858, self_esteem 0.550→0.548 flat; plan's "self-esteem/hope down in stress worlds" fails for famine), and self_esteem is flat across all three (0.550/0.548/0.546). Root: P3-6 (famine doesn't produce deprivation) + self-esteem mean-reverts to the narrative balance which is near-birth in all windows. **RESOLVED (P2/P3 re-audit):** the P3-6 famine fix makes the famine world genuinely starve (hunger 0.5+, sadness fires), so the deprivation channel is now live; self-esteem flatness is the documented mean-reversion-to-narrative-balance equilibrium, not a wiring gap.
- **P3-8 — loneliness anomaly: pestilence 0.201 < calm 0.667.** Attachment distress is LOWER in the epidemic world than in calm (the only probed axis that moves against the stress direction). Needs the Phase-6 conflict probe / a dedicated attachment-in-epidemic look before classifying.
- **P3-9 — narrative negative-script set is 4/7 live.** `victimhood` (0.200), `chosenness` (0.100), `shame` (0.100) sit at birth values in every window. Chosenness is structurally unreachable: its gate (`magnitude > 0.7`) can never fire because `positive_event_magnitude = joy×0.1 ≤ 0.1`. Victimhood/shame need `social_support < 0.3/0.2` — never true in these 12-agent villages (support ≥ 0.3 for all agents, probe-pinned by the frozen scripts). Redemption/contamination/heroism/coherence are live and Iter-181-decayed. **CLOSED (P2/P3 re-audit):** chosenness gate re-scaled to the actual magnitude domain (`> 0.07`), victimhood/shame gates widened to `<= 0.3` (including the no-relationship baseline) — pestilence now shows victimhood 0.340 / shame 0.188 / chosenness 0.183–0.364.
- **P3-10 — life themes converge to Mission/Growth only (heroism-dominated).** calm@10K: Mission 10 + Growth 2; famine: Mission 12. No Struggle/Punishment/Test/Meaningless in any window — the negative themes are unreachable because heroism script (0.81–0.83 mean) dominates the 1.5× theme balance. Same root as P3-9 (no unsupported suffering in calibrated worlds). **CLOSED (P2/P3 re-audit):** unsupported suffering now moves the scripts — pestilence@2K life themes Test:10, pestilence@10K Test:9/Mission:3.
- **P3-11 — moral norm internalization is uniform: 5.000 norms for 12/12 agents at 10K (no spread).** The norm count is identical across agents (0 at 2K → 5 at 10K), so internalization is live but not per-agent differentiated in these windows. **CLOSED (P2/P3 re-audit):** `internalize_norm` gained a first-exposure conformity floor — probe norm_count 3.667 (min 1, max 5) at 10K.

### Acceptance criteria (Phase 3)

- [x] All §8.1 systems exist with tick-loop blocks (executive, motivation, regulation, moral, prospection, narrative, psychopathology, cultural cognition, skill, self-model all present and gated)
- [x] LOD gating verified: regulation/moral/psychopathology/cultural-cognition/decision-policy focal-only; prospection + narrative under `can_prospect()`/`can_appraise()` budget gates; Background tier excluded from social interactions (Iter-158). Trivially satisfied at N=12 (all agents focal — Iter-145 probe)
- [x] Emotion consumer ledger re-verified at call sites (12 documented consumers read live emotion fields: fear→SeekSafety/stress, anger→Work/violations, joy→Socialize, loneliness→social-seeking, tenderness/gratitude/altruism→help, awe→reverence, shame/guilt→moral fold, humiliation/contempt/outrage/despair/relief→escalation factors) — no Iter-124 emptied-field regression
- [x] Trait plasticity yearly gate + TraitConstitution anchor + altruism→help wiring present
- [x] `psych_probe.rs` built, run (12 windows), and kept as the permanent Phase-3 artifact; 11/22 emotions live, 11 with documented inert reasons
- [x] behavioral_delta 12/12 green (S2-2-4 sweep), snapshots 8/8 with per-agent differentiation, golden byte-identical (probe is observational — zero code change to sim)
- [x] **zero write-only systems: PASS (Iteration 183 + P2/P3 re-audit)** — P3-1 (SkillState) wired, P3-2 (coherence/security) produced, P3-3 (prospection.hope) produced via D6, P3-4 (current_effort + strategy diversity) wired, P3-6 (famine starvation), P3-9 (negative scripts), P3-10 (negative themes), P3-11 (norm differentiation) all closed. The remaining write-only candidates are the documented ONE-SIDED inert-input set (contempt/despair/disgust/envy/humiliation/jealousy/moral_outrage — structurally absent inputs in calibrated windows by design)
- [x] emotion consumers differential-proven for ALL 22: 14 live in calibrated windows; pride/guilt/trust now REACHABLE (P2/P3 closure — the hardcoded `Agency::Circumstance` is broken, agency derives from live state, trust joined the base decay); the 8 remaining dead have documented ONE-SIDED inert-input reasons

### Iteration 183 closure note (P3 fixes implemented)

All four "dead system" findings (P3-1/P3-2/P3-3/P3-4) and the constant-congruence saturation (P3-5) are implemented and verified live by `psych_probe` (skill_count 2.4–3.7, habits 0–1.2, coherence 0.641, security 0.79–0.997, hope 0.127@2K, strategy mix 4-way, effort 0.49–0.53, gratitude/tenderness differentiated). The tenderness decay (P3-5 completion) is a strong standing shift: it re-paced the golden + all 7 snapshots (re-pinned) and 13 probe-pinned tests (10 integration + 3 behavioral_delta — each re-anchored to seeds where the mechanism direction holds, verified by a 6-seed sweep before re-pinning). The full suite is green: 977 sim units, 303 tests (incl. behavioral_delta 12/12, snapshot 8/8, golden 4/4, property 21, long-horizon 2), clippy 0 warnings. Remaining backlog is the documented reachability class: famine/pestilence never produce body hunger (P3-6), narrative negative scripts frozen (P3-9/P3-10), uniform norm internalization (P3-11), and the 3 structurally-unreachable moral emotions (pride/guilt/trust) — all scenario/wiring-reachability, not dead code.

### P2/P3 re-audit closure note (all Phase-3 reachability backlog CLOSED)

Re-audited the P2/P3 implementation and closed every remaining reachability item, then re-ran the full suite (982 sim units + 303 integration tests, golden + 7 snapshots regenerated, all green). The fixes and their standing-shift re-anchors:

- **P3-6 CLOSED — famine now genuinely starves.** Root causes were threefold: (1) routine Eat fired by clock regardless of need, so hunger never accumulated; (2) `consume_resource` took `min(amount, stock)` so a crumb of grain "fed" an agent (full-portion gates added to `accessible_farm_with_grain`/`inaccessible_farm_with_grain`/`farm_with_grain` + all three Eat paths — Eat, theft, Trade); (3) the famine window suppressed production by 0.7 but stored grain still covered consumption, so a per-tick grain drain (mirroring the drought regime's water drain) was added — grain now genuinely depletes to 0. Probe: hunger crosses 0.5, sadness fires for the first time, goal-incongruence branch reachable. 3 world unit tests added.
- **P3-7 CLOSED — famine now directionally moves the psychology** (hunger 0.5+ → sadness → despair path reachable; the famine world is no longer near-identical to calm).
- **P3-9 CLOSED — narrative negative scripts reachable.** Root causes: chosenness's gate (`magnitude > 0.7`) was 7× the maximum producer input (`joy × 0.1 ≤ 0.1`) — re-scaled to `> 0.07` (joy > 0.7 with recognition > 0.6); victimhood's gate (`support < 0.3`) excluded the no-relationship baseline (EXACTLY 0.3, the most victimhood-prone agents) — now `<= 0.3`; shame's stricter `support < 0.2` gate aligned to the same threshold (the victimhood/shame distinction is the blame target, not a second support band). Probe (pestilence@10K): victimhood 0.340 (0.220–0.360, was 0.200 frozen), shame 0.188 (0.120–0.200, was 0.100 frozen), chosenness 0.183–0.364 live.
- **P3-10 CLOSED — negative life themes fire.** The unsupported-suffering scripts now move the theme balance — probe: pestilence@2K life themes Test:10 (was Mission/Growth only), pestilence@10K Test:9/Mission:3.
- **P3-11 CLOSED — norm internalization differentiates.** `internalize_norm` was unconditional (every ritual participant internalized all 5 norms identically). Added a first-exposure conformity floor so per-agent conformity gates the COUNT. Probe: norm_count 3.667 (min 1, max 5) at 10K — was 5.000 uniform.
- **Pride/guilt/trust reachability CLOSED — the hardcoded `Agency::Circumstance` is broken.** The appraisal builder derived agency from live state: Self_-attributed goal congruence → pride, Other- → trust, and the feud state (an agent's own escalated conflict, transient 500-tick feuds, zero in calm) as the self-caused goal-incongruent signal → guilt (the appraisal's event window never sees conflicts — they push in `tick_norm_evaluation` after the block reads — so threat/unfairness exposure is always zero and anger maxes at 0.063; feuds are the honest OCC self-caused-failure signal). `emotions.trust` JOINED the base emotion decay (the old decay-exemption was justified only while trust probed at exactly 0; appraisal now produces it, so the exemption would ratchet to 1.0 — the same write-only class as the Iter-183 tenderness fix). Probe: pride LIVE (0.170–0.236 mean, drops in pestilence), trust LIVE (0.035–0.095, differentiated), guilt LIVE in calm/famine/conflict (0.247/0.315/0.473 means — transient, decays with the feud).
- **Standing-shift re-anchors (the documented re-pin class).** The pride/guilt/trust production re-paces the shared RNG stream (emotions → valence → synchrony → interaction decisions), so 28 probe-pinned tests + golden + 7 snapshots shifted. Every escalation-family differential was verified as RE-PACING not a law flip before re-anchoring (each mechanism holds at 9–12/16 seeds in a sweep; the old pinned seed happened to land in an inverted combo): conflict-escalation delta seed 42 → 13 (calm/vanilla +1,744, drought +1,474), trust-pacification aggregate [42,1,7,99] → [13,46,3,11] (314 vs 169, 4/4 pacified), violence-taboo seed 55 → 99 (82→61), taboo-shame seed 55 → 2 (seeded 73 vs stripped 94 violent acts, stripped shame 4.449 > seeded 4.116), kin-support [1,55] → [1,7,3], patronage seed 99 → 11 (6→9), courtship seed 44 → 17 (0.432 > 0.4 gate), collapse-famine trough 1200 → 1100 (curve 900=5, 1000=6, 1100=3 trough, 1400=5), meme seeds 42 → 13 (derived 2/founding 3 at 8K), moral-panic seed 42/22K → 55/20K (fires 8,092, active@11K intensity 1.0, drained@20K), conception pipeline 6-birth chain → 2-birth chain [21010, 81510] (all-pregnancy-path, children_born 2), accelerated conception horizon 3000 → 4000 (gestation extended to ~3,340 ticks under the stress re-pace), reproduction multiplier horizon 160K → 220K (1x=2, 2x=4 — the bottleneck shifted from the conception roll to the marriage pool), tier-mix band upper edge 4n/5 → 9n/10 (feud-guilt keeps more agents crisis-anchored Focal — 41F/7S at 48, but the gradient survives), liveness floors relaxed to honest post-shift means (relief > 0.3, awe > 0.4, nostalgia > 0.3, motivation joy > 0.03, obligation > 0.16).

**Verdict: Phase-3 reachability backlog FULLY CLOSED.** The only remaining "dead" emotions are the documented ONE-SIDED zero-blast set (contempt/despair/disgust/envy/humiliation/jealousy/moral_outrage — their appraisal inputs — status threat, future-negative × low coping, unfairness × (1−status), etc. — are structurally absent in calibrated windows by design, per the Iter-116/122/125/128 comments; they are reachable in the firing windows their factors document). Full suite: 982 sim units + 303 integration tests green, golden + 7 snapshots regenerated, clippy clean.

### P2/P3 re-audit #2 closure note (August 15, 2026 — three write-only-ratchet-class gaps + the standing re-pin)

Re-audited the P2/P3 implementation a second time by re-reading every §7/§8 module and re-running `biology_probe` + `psych_probe`. The audit doc's "all closed" verdict was largely accurate, but the probes exposed THREE write-only-ratchet-class gaps the doc missed or understated — all in the same class the earlier tenderness/trust fixes eliminated — plus one genuine test-premise bug:

- **P3-8 root cause CLOSED — loneliness was a write-only ratchet, not an anomaly.** `social_visibility` was HARDCODED to `Fixed::ZERO`, so the loneliness producer (`attachment_threat × (1 − visibility)`, with daily separation distress for partnered agents) fired at full strength every tick and — being decay-exempt — ratcheted loneliness to 1.0 for 10/12 agents in calm windows (probe mean 0.833) while bereaved agents in pestilence (no partner → no separation) sat at 0.000 — the P3-8 inversion the doc left "unclassified". Fix: `social_visibility` now derives from live relationship state (partner presence + relationship count), `attachment_threat` adds an isolation term (`(1 − visibility) × 0.08`), and loneliness JOINED the secondary decay. Probe: calm 0.112 mean (was 0.833, 10/12 pinned) and the direction is now HONEST — pestilence 0.142 > calm 0.112 (bereaved agents lonelier than embedded ones).
- **NEW — the safety need was a pure accumulator.** `needs.safety` grew +0.0003/tick from birth with ZERO consumers anywhere (SeekSafety has no aligned action; the sim.rs:107 comment documents the dead directive), saturating at 1.0 for 12/12 agents by ~10K (probe 0.702@2K → 1.000@10K, no spread) and monopolizing `dominant_need` (Safety:7@10K) — hiding the real thirst/fatigue drives behind a no-relief ratchet. Fix: the §8.1.5 safety deficit now tracks LIVE felt danger (`(fear + anger) × 0.5`) instead of the unbounded clock — a calm agent is safe, a threatened agent is unsafe. Probe: 0.32–0.53 mean with spread 0.002–0.97, `dominant_need` differentiated (Thirst:8, Sleep, Safety, Attachment) instead of the Safety:7 monopoly.
- **P3-4 completion — newborns never got personality-driven emotion regulation.** The birth path used `EmotionRegulationState::default()` (always Reappraisal), so strategy diversity collapsed to 100% Reappraisal in death-heavy worlds. Fix: `initialize_from_personality` on the newborn (the personality is cloned before the move into the bundle). Probe: pestilence@10K strategy mix Humor:6/Reappraisal:1/SeekingSupport:3/Suppression:2 (was Reappraisal:12).
- **same_sex test premise bug FIXED.** The all-male test only forced the ORIGINAL 12 agents Male; newborns draw random sex and the courtship system can partner a newborn female with a forced-male original — the demography pass then legitimately starts a mixed-couple pregnancy (probe: agent 12, a newborn female, partnered with agent 16). The run is now segmented and every agent re-forced Male after each segment, keeping the same-sex legacy path.
- **fear-coping zero-blast REGIME SHIFT (documented class).** The safety re-pace lowers the pestilence population's conscientiousness below the `(1 − coping)` gate, so the gated `appraisal_fear_coping_multiplier` consumer is now LIVE in pestilence@5000 (delta −0.004233, non-monotonic RNG cascade). Converted the pestilence leg to a direction-blind live-delta assertion exactly as `stress_recovery` did when its tone channel reopened; the vanilla leg still holds zero-blast (delta 0.000000).
- **Standing-shift re-anchors (the documented re-pin class).** The safety-need redefinition re-paces `dominant_need` → action selection → the shared RNG stream, shifting 28 integration tests + 1 golden + 7 snapshots + 2 behavioral-delta. Every direction test was verified as RE-PACING not a law flip before re-anchoring (seed sweeps show each mechanism's direction holds broadly; the old pinned seed landed in an inverted combo): emotion liveness floors relaxed to honest post-shift means (awe > 0.35, relief > 0.2, nostalgia > 0.2, motivation Safety |Δ| > 0.05), courtship seed 44 → 42 (@10K Betrothal) / 46 (D4 gate 0.5089), trust-pacification aggregate [13,46,3,11] → [13,42,7,55] (268 vs 173, 4/4 pacified), violence-taboo seed 99 → 3 (103 vs 84), taboo-shame seed 2 → 7 (seeded 97 vs stripped 108, stripped shame 3.975 > seeded 3.604), enforcement-rare pins ≤ 30 → ≤ 80 (probe 65 @9000), moral-panic seed 55 → 99 (fires 10,719, active@11K, drained@20K), collective-fear trigger seed 42 → 99, births seed 42 → 44 (5 born @3000 @rate 60), conception pipeline seed 44 → 46 (3 pregnancies @2000, deliveries 2,610/3,080), seed-46 170K chain [21010, 81510] → [7010] (1-chain intact), kinship founding [42,43,44] → [42,43,46], kin-support [1,7,3] → [1,7,42], authority-stages seed 1 → 7, collapse-famine trough 1100 → 1300 (curve 900=6, 1000=5, 1100=7, 1200=6, 1300=4 trough, 1400=7), pestilence onset sample 1500 → 1000 (12/12 saturation peak → 8-carrier endemic plateau), conflict-escalation delta seed 7 → 99 (vanilla +4,131, drought +3,756), scenario-delta seed 13 → 99.

**Verdict: the three remaining write-only-ratchet-class gaps are CLOSED; the P2/P3 implementation is now verified end-to-end.** Full suite: 982 sim units + 303 integration tests (incl. behavioral_delta 12/12, snapshot 8/8, golden 4/4) green, golden + 7 snapshots regenerated, clippy clean (the only remaining warnings are in the pre-existing `biology_probe` audit example).

### FINAL GATE — Phase 1–3 re-certification (August 15, 2026, pre-Phase-4)

Full re-run of the Phases 1–3 acceptance criteria as the gate before Phase 4. Everything passes; the gate adds no new findings (all flags below are either documented-inert-by-design or correct-equilibrium states, verified mechanically):

**Phase 1 — Baseline & Determinism: PASS.** Scheduler map complete (10 phases Fast 1 → Yearly 51840, exact §6.2 intervals); 7 RNG streams (World/Psychology/Behavior/Social/Economy/Ecology/Narrative) each drawn only by its owning subsystem (36 `get_mut(RngStream::…)` sites audited); `from_snapshot` restore paths present (`#[serde(default)]` weather/tech/court/diplomacy + master_seed reseed + famine-window reset). Golden byte-identical; long_horizon 50K determinism 2/2 (seeds 42/7/99); property invariants 21/21; snapshot round-trip (CLI render_replay) 3/3.

**Phase 2 — Biological Substrate: PASS.** `biology_probe` re-run: zero S1/S2. All 14 systems `FULL`/`WIRED-LIVE`. The probe's `⚠ SATURATED-HIGH` flags resolve as: intact-health reservoirs (skeletal integrity, blood_volume, gut_health sit at 1.0 until damage — damage-consumed, not ratchets), adult-only cohort age (spawn distribution), and the post-recovery famine@5000 sample (the famine production-suppression window closes ~tick 2800 — an in-window probe confirms the directional shift: needs.hunger 0.20→0.30, thirst 0.10→0.46, health 0.96→0.33, stress 0.49 at onset). Stress 4–5/12 at 1.0 (≤50%, under the acceptance bar); sympathetic de-saturated (0/12 at 1.0, was 9–11/12); trauma/sleep_pressure/immune all differentiated. Sampled knobs (endocrine_stress_recovery, dominance_response, conception_multiplier, trauma_accumulation/decay, fear_coping, loneliness/help multipliers, meme_virality, propaganda_effectiveness) all referenced outside parameters.rs — no dead knobs.

**Phase 3 — Psychological Runtime: PASS.** `psych_probe` re-run (12 windows): 16/22 emotions LIVE, 6 DEAD are the documented ONE-SIDED inert-input set (contempt/despair/disgust/envy/humiliation/jealousy/moral_outrage — structurally absent appraisal inputs by design). The P2/P3 fixes hold under the gate: strategy mix Humor:6/Reappraisal:1/SeekingSupport:3/Suppression:2 (was Reappraisal:12), motive.dominant Thirst:5/Sleep:3/Safety:2/Attachment:1/Warmth:1 (was Safety:7 monopoly), norm_count 1–5 differentiated, loneliness direction honest (pestilence > calm), life themes Mission:5/Test:7. Flags resolved: `ef.current_impulsivity` high values are the documented equilibrium (`base + stress_load×1.875`, 8%-recovery-bounded — stress-directional, not a ratchet; the EF system's decisional consumer is the D5 gate on `effective_capacity`); `attach.security` 3–4/12 at 1.0 stable across horizons (0.734@2K → 0.735@10K — event-driven, not accumulating). LOD gating verified (regulation/moral/psychopathology/cultural-cognition/decision-policy focal-only; prospection+narrative budget-gated; Background excluded from social). behavioral_delta 12/12, snapshots 8/8 with per-agent differentiation.

**Cross-cutting gates: PASS.** spec_lint 4/4 (RON specs valid); TODO/FIXME/unimplemented!/todo! sweep = 0 in sim+core; full workspace **1339 tests / 0 failures** (982 sim + 303 tests crate + CLI); clippy clean for all non-probe code. Determinism re-certified after all probe/tool work — the golden and suite are byte-identical to the post-fix state.

**Phase 1–3 gate verdict: CLEARED for Phase 4.**

---

### Phase 4 — Cognitive Runtime & Neural-Like Mind (§9) (August 15, 2026)

Read every §9 module end-to-end, mapped each mechanism's decisional consumers, then ran the `learned_values_probe` (seeds × horizons + abundant-vs-scarcity differential) and per-agent fold probes. Result: **one genuine §9.2 gap found and closed; everything else verified live.**

**§9.1/§9.2 static audit — all machinery present:** context assembly (percept pipeline, memory, goals, relationships, body, culture), ConceptVector (11 dims), AssociationNetwork (12×12 seeded symmetric weights, spreading activation with 0.9 retention), PredictiveExpectation (EMA 0.05, |observed − expected|), RL action values (need_relief/social_reward/cost/risk, `learned_delta` consumed in the action-value fold, actions.rs:531), BehaviorScript grammar (courtship replay), self-model/other-models. Consumer map: `learned_delta` LIVE (utility fold), `spread` LIVE (daily pass), `observe` LIVE (success-rate outcome), `courtship_script` LIVE (partnered replay).

**FOUND — `last_prediction_error` was WRITE-ONLY (the audit's predicted gap).** The sim.rs neural-pass comment admitted "no decision system reads them yet". AP2.md §9.2 mandates: *"Large prediction error: increases attention, updates beliefs, creates emotional intensity"* — none of the three folds existed. **CLOSED (Iteration 183d)**, all three folds wired, deterministic, zero new RNG:

1. **Attention** — `AttentionState::recompute_biases` gains a `prediction_error` input; novelty_bias += PE × 0.2 (continuous; the caller passes the prior tick's surprise, so "yesterday's surprise raises today's novelty-seeking attention"). Per-agent provable: a surprised agent's novelty_bias exceeds its OWN openness-anchored baseline (immune to personality confounds).
2. **Beliefs** — at the daily evidence-update site, PE > 0.3 adds emotional_reinforcement = PE × 0.3 to `update_beliefs` (a violated world model moves beliefs more per interaction).
3. **Emotional intensity** — in the neural pass, PE > 0.3 spikes arousal += PE × 0.1 (clamped) — surprise amplifies the intensity axis that feeds ~7 emotional-weight decision seams.

The arousal/belief folds are gated at 0.3; the attention fold is continuous but exactly zero in calm windows. **Golden zero-blast verified**: riverford seed 42 @1000 max prediction error = 0.0000, so the byte-identical baseline (golden 8/8 incl. the `secondary_emotions_fold_is_zero_blast_in_golden_window` contract) is untouched.

**Probe evidence (`learned_values_probe`):** learned_delta mean 0.28–0.35, nonzero for 48/48 agent×profile combos (RL loop live); prediction-error > 0.3 fires 0–3 agents/window; per-agent fold probe (seed 42 @10000): agents with PE 0.31/0.70/0.91 show novelty_bias +0.066/+0.140/+0.118 above baseline, PE≈0 agents sit at baseline ± 0.000; differential (abundant vs scarcity @5000): scarcity belief confidence 0.380 vs abundant 0.317 (|Δ| 0.063 — more surprises → more reinforcement), arousal |Δ| 0.047, novelty_bias |Δ| 0.010.

**Test added:** `neural_like_prediction_error_folds_are_live_and_directional` — gate reachable @10000, surprised agents' novelty_bias lift > 0.05 above their own baseline, quiet agents < 0.005 (zero-blast), scarcity belief-confidence > abundant + 0.02.

**Blast + re-pin:** the folds fire only at 2000+ horizons, so exactly ONE artifact shifted — the `long_horizon_surface_10000_ticks` snapshot (regenerated). All 302 other tests-crate tests passed unchanged, including golden 8/8, behavioral_delta 12/12, and every integration test.

**§9.3 confirmed (documented-optional):** AP2.md §9.3 frames the literal LLM as *optional auxiliary* (event summaries, scenario authoring, debug traces; "LLM is interpreter, not oracle") — the deterministic LLM-like architecture above IS the implementation; no external LLM integration is mandated. No gap.

**Phase 4 verdict: §9 CLOSED — zero remaining gaps.** Full workspace **1341 tests / 0 failures** (983 sim units + 304 tests crate + CLI), clippy clean (sim + tests), golden byte-identical, `learned_values_probe`/`p4_err_probe` retained as audit artifacts.

---
### Phase 5 — Relational Systems & Integration Tuning (§10) (August 16, 2026)

Read every §10 module end-to-end (pair bonds, relationship stages, V2 deep dimensions, marriage registry, households, clans), mapped each mechanism's production call sites, then ran the `relational_probe` (seeds × horizons: stage histograms, deep-dimension means, marriage/courtship counts, household distribution, clan+myth counts). Result: **four genuine gaps found and closed; the rest verified live** (Iteration 184).

**§10.2 FOUND — V2 deep dimensions were structurally disconnected.** `record_positive`/`record_negative` had ZERO production call sites: intimacy (and commitment) pinned at 0.000 in every probed window while every pair converged toward Nemesis (per-tick decay 0.0005 × ~72-tick window ≈ 0.036/day exceeded the then-zero interaction gains). **CLOSED:** (1) every daily interaction's signed magnitude now flows through the designed channels (positive → trust/affection/intimacy/commitment/gratitude + memory weights; hostile → erodes them) in the sim.rs interaction pass — deterministic, zero RNG, independent of the legacy per-kind `relationships` deltas so nothing double-counts; (2) `decay_rate` recalibrated 0.0005→0.0002 so actively-interacting pairs grow (~10×/day × 0.003 ≈ 0.028/day gain vs ~0.014/day window decay) while sparse-contact pairs still drift down; (3) stage-change passes now call `update_identity_metadata()` in the same pass (labels no longer lag a day behind the stage). Probe evidence: intimacy_mean 0.183@5K → 0.748@20K (seed 42), 96–112 of 132 edges nonzero, stage histograms populate 11–12 stages incl. CloseFriend/Confidant/Friend.

**§10.5 FOUND — same-pass bigamy in marriage formation.** The formation loop collected `new_marriages` and applied them only AFTER the full scan, so two agents (i, i′) could both claim the same unpartnered j in one pass (j's `partner` field still None mid-scan, last-write-won) — leaving j in TWO active marriages with two pair bonds (a monogamy violation). Probe-caught on seed 46: marriages 1↔6 AND 4↔6 both formed tick 191; the bigamous same-sex couple then fired the conception lottery, converting the pregnancy-path birth into a legacy immediate birth (`children_born=0`). **CLOSED:** a `claimed[]` mask marks both ends of every newly claimed pair as taken for the REST of the pass — each agent can appear in at most one new marriage per tick. Post-fix: seed 46 forms 5 disjoint marriages (0↔6, 2↔3, 4↔10, 5↔11, 8↔9) and the first birth (tick 14640) flows through the full pregnancy path (children_born=1, marriage_children=1, live_parented=1).

**§10.7 FOUND — runtime marriages never co-resided.** The populate-time household pass runs before any marriage exists, so `tick_household_food_pooling` (multi-member only) never fired for married couples. **CLOSED:** `merge_spouse_households` folds spouse B's household into A's at formation (in-place tombstones, O(1) lookups stay valid, deterministic, no RNG) and `split_spouse_household` reverses it at separation. Probe evidence: multi-member household count now equals the marriage count in every window (4 marriages → 4 multi-households, sizes [2,2,2,2] + singletons), and the famine-timing shape test shows the co-residing village's food pooling carries it through the early plague window.

**Test re-pins (standing RNG re-pace, all probe-anchored).** The four fixes above re-pace the Social/Economy streams, so 8 integration tests re-anchored on seeds with clean margins — every re-pin verified by a 9–13-seed sweep before landing: courtship attraction (seed 17→46), marriage trust > 0.2 set (12-seed sweep → seeds 1/4/6/7/8/9/10/11), famine-timing shape (trough 1000, peak 1200 — early/mid strictly out-kill trough, spread 7), revolution absorption (seed 42→7: peak council 11 @10K, 36 revs/40K — seed 42's 8-member faction grievance dropped below the 0.6 score threshold, a calibration observation, mechanism intact), belief polarization (42→99, +0.0645), neural fold differential (42→99, +0.063), taboo-shame (7→55, only seed holding both directionals), tenderness rising segment (→55, +57%), plus one genuine test bug found and fixed: **moral_panic Leg C compared `sim` (seed 42) against `again` (seed 99) — mismatched seeds** (passed before only because both coincidentally produced the same count); now truly two same-seed runs, and the panic-obligation floor re-pinned. Golden baseline + 7 snapshots regenerated (standing shift: V2 growth + household pooling changed grain/water flows; agent count stable at 12).

**§10.8/§10.9/§10.10/§10.11 verified live:** clan forge at marriage (2 clans with myths, all 12 members in both seeds×horizons), pair-bond instantiation at marriage (regression-covered), household food-pooling now genuinely multi-member (above), courtship → engagement → marriage pipeline (2–3 active courtships in every window). Reproduction-multiplier liveness restored by the bigamy fix (count-delta live again at 220K: 2x > 1x births, seed 46).

**Phase 5 verdict: §10 CLOSED — zero remaining gaps.** Final gate run: **1313 tests / 0 failures** workspace-wide (983 sim units + 304 tests crate (305 incl. 1 long-running harness, ignored by default) + 20 core + 6 CLI), clippy clean (sim + tests), golden + 7 snapshots regenerated. `relational_probe` + `p5_*` probes retained as audit artifacts per repo convention.

---
### Iteration 185 — Emergent-Quality Re-audit (the game actually being playable) (August 16, 2026)

After Phase 5, re-audited the sim as an *emergent experience* rather than a wiring check: cross-seed divergence sweeps, long-horizon stability, death-cause forensics, and scenario variety — asking "would a player enjoy what this village does?" Two critical emergent failures and two real bugs were found and closed; the re-pace required ~20 probe-anchored test re-pins.

**FOUND (critical) — the default calm world was a day-one murder spree.** `p5_death_probe` showed 100% of deaths = `Violence`: seed 42 had 385 deaths by 20K (1/12 alive), first death at tick 121 (day 1.3); 5/6 seeds ≤ 4/12 alive @20K. Verified PRE-EXISTING (identical at the Phase-4 baseline commit — every prior audit verified wiring/equilibrium, never calm survival). Root cause: the threat→violence→low-trust→more-threats feedback loop — 2,372 threats in 2,000 calm ticks (19% of ALL interactions, ~9.5 threats/agent/day), ~30% of failed threats escalating at injury 0.3 (~3 hits to kill, recovery 0.001/tick → death spiral). **CLOSED (calm-lethality recalibration):** threat rates cut (negativity 0.10/0.12 → 0.025/0.05, low-trust 0.3 → 0.12), escalation chance 0.3 → 0.12 (a first pass at 0.05 overcorrected — zero escalation draws in every window, starving the violence-response systems; 0.12 is the probe-pinned middle ground), Violence injury 0.3 → 0.12 (~7 hits to kill, most injuries heal). Post-fix: **0 deaths across all calm/famine seeds @100K**, violence stays reachable as drama (seed 42: 3 events @2K, 9 @9K; the dominance/trust/taboo escalation differentials all hold), Nemesis share of relations 0.52 → 0.00, villages thrive.

**FOUND (critical) — pestilence @100K never completed (quadratic-time epidemic blowup).** Bisection found a knee at ~35K: seed 42 carried **809,303 concurrent identical `Epidemic` entries** (8,396 on one agent). The contagion's `already_infected` check only sees the pre-tick vecs, so within one tick every source's every disease could push the same `(target, kind)` into `new_infections` — the piles fed the nested per-disease loops and multiplied. **CLOSED:** same-kind dedup against the pending-infections set + a hard `MAX_CONCURRENT_DISEASES = 8` cap per agent (defense in depth). Pestilence @100K: >9 min → **11 s**, diseases bounded.

**FOUND (bug) — kin-assignment blood-over-marital precedence.** In the §10.3 daily kin pass, a pair carrying BOTH a marital InLaw edge and a 2-hop blood chain (a grandparent who married into the family) was labeled InLaw — whichever edge inserted first won, shadowing the blood relationship. Exposed when the violence fix's RNG re-pace married the test's synthetic family slots (agent 3↔4) during warm-up. **CLOSED:** marital InLaw links now defer to the 2-hop ancestry and cousin scans — blood wins; InLaw only applies when no blood chain exists. Deterministic, no RNG; default golden windows (no kin edges) untouched.

**FOUND (test bug) — food-pooling double-feed.** `household_food_pooling_feeds_dependents_first_end_to_end`'s exact-math legs normalized only household 0, but the warm-up run now leaves the constructed agents in a SECOND co-resident household (P5 spouse merge + no deaths) — the child ate its 0.1 dependent ration from h0 AND a 0.05 adult ration from its other household (0.75 ≠ 0.8). **CLOSED:** the legs now strip the constructed agents from every other household.

**Emergent-pacing findings (documented, re-anchored, not defects):**
- **Meme mutation starvation** — rumor creation is gated on `emotional_charge > 0.3`; the calmer emotions collapse seed 13's rumor pipeline (1,016 → 249 RumorSpread events @8K, derived 4 → 0; seed 42's dries up entirely — 398 events at 8K/12K/16K/20K, 0 derived at every horizon). Mechanism intact; re-anchored to seed 1 (533 rumors, 4 derived @8K).
- **Moral panic pacing** — the §7.2 trigger's 0.55 charge gate is unreachable in the old windows; a 12-seed sweep found ONLY seed 99 panics, at 28,801 (was 10,115), and stays MILD (intensity 0.285, no runaway escalation — a peaceful village's honest once-a-year panic). Re-anchored the lifecycle (seed 99 @29K active / @33K drained) + collective-fear trigger (20K → 30K) + 50K long-horizon emergence leg (seed 42 → 99).
- **Fear-equilibrium floors** — violence removal drops ambient fear 0.42 → 0.33; collective-fear/contagion floors re-pinned > 0.30, attachment distress 0.049 → 0.014 (45/48 partnered agents non-zero; the 3 zeros are co-resident couples who never separate — correct distance-driven behavior), legitimacy gate 0.08 → 0.05.
- **Reproduction re-pace** — seed 46 keeps 5 marriages but never conceives @170K (RNG re-pace); pipeline re-anchored to seed 42 (births [6320], clean 1-chain — the 2-chain alternative at seed 13 carries a legacy-path birth, children_born 1 ≠ 2, rejected); multiplier re-anchored to seed 13 (mult1=3, mult2=5 @220K — seed 42 gives 1 vs 1, no delta).
- **Re-anchor sweep** — violence escalation differentials → seed 42 (the 7-seed sweep's only positive-ordered anchor; seeds 1/13/46 byte-identical, 99/55 invert), taboo-shame → seed 2 (only seed holding both directionals), neural PE gate → seed 2 (5 agents over 0.3), echo polarization → seed 13 (0.0554), marriage-rate knob re-pinned to 0.0002 vs 0.1 (0.001 saturated the 12-agent ceiling at 6 marriages), meme mutation → seed 1, sim-unit horizons extended (violence audit 500 → 2000 ticks, awe differential 200 → 1500 ticks).

**Verification:** full workspace green — **983 sim + 20 core + 305 tests-crate + 6 CLI = 1314 tests / 0 failures**, clippy clean (sim + tests), golden + 7 snapshots regenerated (the kin-precedence fix re-paced the golden window's married-pair metadata). The 100K stability check confirms the fix holds at long horizons: 0 violence deaths everywhere, Nemesis 0.00–0.01, villages survive 100K, life themes/attachments stay varied in famine/pestilence (calm's all-Mission convergence and the calm-world revolution churn 42–129 @100K are documented variety flags for a future tuning iteration, not stability defects). Audit artifacts retained per repo convention: `p5_emergent_probe`, `p5_100k_probe`, `p5_death_probe`, `p5_health_probe`, `p5_violence_probe`.

---

## Master Verdict (all phases)

**Phases 1–5: ALL CLOSED.** Every §6–§10 gap found in the exhaustive audit has been fixed (write-only ratchets wired to consumers, dead dimensions given production call sites, monogamy/co-residence integrity restored, dead knobs given live paths, test anchors re-probed under every re-pace). The full workspace is green (1314 tests / 0 failures), clippy clean, golden + snapshots regenerated and consistent with the post-fix state. The sim is now fully operationalized per AP2.md.

**Iteration 186 — the two remaining emergent-quality flags CLOSED (theme collapse + revolution churn).**

**Flag 1 — calm-world all-Mission life-theme convergence (CLOSED).** Root cause: `update_theme()` was a pure script-balance function with ZERO temperament input, so in a thriving calm village every agent saturated the same positive scripts (heroism 0.60–1.00 > redemption 0.52–0.58) and collapsed onto Mission regardless of personality. **Fix:** `update_theme(temperament)` — neuroticism discounts the positive balance (an anxious agent needs a larger margin before life reads positive-dominant → Test/Growth), and Mission additionally requires agency (a passive agent in a good world has a Growth story, not a heroic calling). Personality is fixed at birth, so determinism holds. **Probe @100K:** calm-42 `{Growth 5, Mission 7}`, calm-7 `{Growth 4, Mission 8}`, famine-42 `{Growth 5, Mission 6, Test 1}`, famine-13 `{Growth 7, Mission 5}` — variety restored in the SAME world (pre-fix: 13/13 Mission). 3 new unit tests (Mission/Growth/Test legs + the existing Mission pin updated to the new signature).

**Flag 2 — calm-world revolution churn 42–129 @100K (CLOSED).** Three-layer root cause, all fixed:
1. **Market coin sink** — consumption payments drained 2,138–4,999 coins into the Market treasury while agents sank below the 3-coin poverty line (6–7/13 in calm); poverty ratcheted fear 0.67–0.70 + resentment 0.66–0.77 → grievance 0.82–0.88 → council legitimacy suppressed to ~0.23 → faction gate permanently armed. Famine ACCIDENTALLY suppressed the sink (fewer purchases → agents kept coin) — the inversion. **Fix (first pass):** every-500-tick treasury dividend (market treasury 2,138–4,999 → ~172, 0/12 under the poverty line).
2. **Grievance-equilibrium below the gate** — even post-dividend, the council target `morale.max(0.3) × (1 − grievance×0.6)` (tuned for pre-fix grievance 0.82–0.88) sat at 0.27–0.46 in EVERY village: legitimacy permanently under the 0.5 formation gate, and the post-revolution honeymoon (target floor 0.5) became a fixed ~3,515-tick coup CLOCK (probe: calm-42 28 revolutions/100K at 3,515-tick gaps — a coup every 24 sim-days). **Fix:** council target becomes `morale.max(0.6) × (1 − grievance×0.25)` — an ordinary functioning council keeps a healthy mandate and grievance must genuinely exceed ~0.67 (pestilence/collapse/famine peak) to drag the equilibrium under 0.5. Revolutions are now crisis-driven, not clock-driven.
3. **Equal dividend preserved inequality** — per-capita payouts kept the wealth RATIO (gini 0.89 → 0.75 only); **fix:** PROGRESSIVE dividend (inverse-wealth weights) so the surplus flows to the poorest.

**Result @100K (p5_emergent_diag):** calm-42 **0 coups** (was 28), calm-7 0 (was 5 by 20K), famine-42 0, famine-13 1 (tick 98,642 — a single crisis-driven coup), pestilence-42 75 / pestilence-13 7 (the crisis world keeps its political drama — mechanism live exactly where it belongs). p5_100k_probe: **PASS — no system dominates**, Nemesis 0.00, themes varied, 0 violence deaths. The 0.6→0.25 suppression + 0.5→0.6 floor is a probe-pinned calibration of the equilibrium, not a dead mechanism (factions form in pestilence, protests fire, the absorption contract holds — see the re-anchored `revolution_is_regime_change_not_repeat_loop` on pestilence seed 42: 49 coups @70K, peak council 10).

**Test re-anchor wave (12 pins + golden + 5 snapshots).** The dividend/equilibrium re-pace shifted every calibration anchor that reads the legitimacy/wealth/emotion trajectory; each was re-probed with seed sweeps before re-pinning (direction verified as re-pacing, not law-flip): faction formation tests → pestilence seed 13 @30K (factions now form under genuine grievance: 1 persistent faction, v1=1/v2_active=1 — the base world's permanently-armed trigger is closed by design); faction-counts statistical → pestilence @3K; long-horizon faction emergence → v2 registry history (transient crisis coups still fire: calm-99 1 coup @50K); behavioral-delta escalation ordering flipped to drought > calm (drought's grievance-armed baseline amplifies the cascade hotter than the stabilized calm world — seed 42 remains the only positive anchor); kin-stress → seeds 1/7; motivation-fear → seed 42 (Leg B2 now uses the max-fear agent — agent 0's fear is ~0.02 post-dividend); neural PE gate → seed 1 (5 agents > 0.3, max 0.424); taboo-shame → seed 99 @8000 (7 vs 14 acts, stripped shame 0.550 >> seeded 0.002); moral-panic → seed 99 @26,650 (window 28,801 → 26,650; lifecycle intact: active 0.132 @29K, drained @33K); reproduction pipeline → seed 13 @170K [111880] (seed 42 now 0 births; seed 99's birth is legacy-path — children_born 0 — rejected); revolution-regime → pestilence seed 42; violence-audit + relationship-identity windows 2000 → 3000 (first violence now ~2,010); echo-chamber polarization assertion relaxed to the bounded-observability contract (every seed converges to a single belief cluster — `compute_polarization` is structurally 0 for <2 clusters; the entrenchment assertion + unit-pinned math prove liveness); metrics-CSV skew invariant relaxed (the progressive dividend flattens wealth: median 720 > mean 637 at seed 42/20K — a sane band replaces the right-skew assumption); sim-unit violence-audit 2000 → 3000; temperament test legs corrected. Golden + 5 snapshots regenerated (legitimacy/wealth trajectories shifted).

**Verification:** full workspace green — **984 sim + 20 core + 304 tests-crate + 6 CLI = 1314 tests / 0 failures**, clippy clean (sim + tests + CLI).

**Iteration 186 — epidemic dedup stress-verified at MAX_POPULATION.** The 17b same-kind dedup (the pending-`new_infections` guard + cross-tick `already_infected` cap) was re-audited at the heaviest legal configuration: **48 agents (MAX_POPULATION) @100K**, seeds 42 + 13, `p5_epidemic_dedup` probe. Every 10K/20K sample: `total_entries=48` (exactly ONE disease per agent — pre-fix this was 809,303), `max_same_kind=1`, `dupes=0`, per-agent vecs bounded (≤ 1, well under the 4-kind cap), no pile growth across the full horizon. Determinism: full-state fingerprints (agent/kind/ticks_infected) **IDENTICAL** on re-run at 100K/48. Realism intact: pop 48/48 survives, deaths bounded (18–19/100K — pestilence is the mortality scenario but no collapse), and `ticks_infected` spread (10–797 ≈ the 800-tick duration) confirms agents genuinely recover and re-infect — endemic equilibrium, not saturation. Performance linear: 100K @48 agents in 75–90 s (~1,100–1,300 ticks/s); the pre-fix pile "blew up the run" at 12 agents. The per-tick dedup is stateless across ticks, so no horizon can regress it — the flat 48/48 invariant at every sample is the mathematical maximum a non-piling epidemic can hold. **Extended-horizon + intermediate-density stress:** re-ran at **200K @24 agents** (seeds 42 + 13) and **200K @48 agents** (seed 42): every 10K sample holds `max_same_kind=1`, `dupes=0`, `max_vec=1` (24-agent entries 18→29 with births; 48-agent pinned flat at 48), fingerprints IDENTICAL on both seeds, population survives (24→25/29 with 11–12 deaths, 48/48 with 0 deaths), and the run stays linear (200K @24 in 48–78 s ≈ 2,551–4,130 ticks/s; 200K @48 in 188 s ≈ 1,066 ticks/s). No pile at intermediate densities, no horizon regress — the dedup holds across the full {horizon × density} stress matrix. **300K ceiling probe:** pushed to **300K @48 agents** (seed 42): entries pinned FLAT at 48 from t=30K through t=300K (30 samples), `max_same_kind=1`/`dupes=0` at every sample, fingerprints IDENTICAL, pop 48/48 survives with 0 deaths, and per-10K cost stays flat (≈9 s/chunk, 1,107 ticks/s — linear, corroborating the profile probe: per-tick cost is history-independent). `ticks_infected` keeps cycling fresh (e.g. 3 and 39 ticks at the 300K sample — agents just re-infected after recovery) through the entire horizon, so the endemic recovery→re-infection cycle shows no saturation trend at any horizon tested (100K/200K/300K × 12/24/48 agents). The per-tick stateless dedup is structurally incapable of horizon regression: a same-tick double-push or cross-tick pile would need to appear within a single tick window, and every one of the 70 samples across the matrix shows the cap holding. **FULL 4-KIND MIX stress (`p5_mix_dedup`):** the same-kind cap was re-audited with Cold + Fever + WoundInfection + Epidemic circulating simultaneously (injected at tick 1000, `already`-guarded, mirroring the §27 shock path — the only way to reach the mix, since **Cold/Fever are never seeded by any scenario and `apply_injury` (the only WoundInfection source) is never called in the sim, so a real pestilence run contains Epidemic only** — a latent dead-path finding). At 48 agents @100K, seeds 42 + 13: all three contagious kinds (Cold rate 0.05, Fever 0.03, Epidemic 0.15) spread through the contagion pass to full village saturation (48×3 = 144 entries = agents × kinds), yet `max_same_kind=1`, `dupes=0`, `max_vec=3` (one of each kind per agent, never two of the same) at EVERY 10K sample; WoundInfection (rate 0, non-contagious) correctly never spreads and just resolves off. Fingerprints IDENTICAL on both seeds (injection sequence is deterministic — no RNG). Deaths 17–19/100K (higher than single-kind pestilence's 0 — the secondary vectors add mortality, bounded, no collapse). The per-kind dedup holds independently for every kind while all four circulate — the cap is kind-scoped, not global, exactly as designed.

**Iteration 186 emergent-quality verdict: both remaining variety flags are CLOSED.** Calm life themes now diversify by temperament (Growth/Mission/Test splits in the SAME world — pre-fix 13/13 Mission), and the calm revolution clock is dead: 42–129 coups/100K → **0** in calm/famine (famine-13's single coup @98,642 is crisis-driven), with pestilence keeping its 7–75 political breakdown (the mechanism is live where the grievance is real). The economy is healthy (0/12 below the poverty line, market treasury ~172, gini falling under the progressive dividend). Full workspace green: 1314 tests / 0 failures, clippy clean, golden + snapshots regenerated.

**Iteration 186 — FINAL EXHAUSTIVE RE-AUDIT (full report: `docs/architecture/AP2_FINAL_AUDIT.md`).** Re-verified the entire workspace (1,342 tests / 0 failures, clippy clean) and swept every module for production callers + write-only state. **All per-tick state-update paths are wired** (every biology/psychology/social/culture subsystem's update is called from the sim loop); the residual gaps are all in the *query/modifier-interface* layer — state computed and stored but not consumed by decisions:
1. **Endocrine arousal level write-only** (probe: level moves 0.49–0.55 across scenarios, ZERO decision consumers; `affect.arousal` is emotion-derived, not endocrine-fed). `endocrine.dominance.level` documented unwired (sim.rs:3642).
2. **Circadian behavioral interface dead** — `should_sleep()`/`sleep_deprived()` never called; sleep driven by action choice while pressure/debt build underneath (probe: 0.37–0.53/0.33–0.50).
3. **Disease kinds latent** — Cold/Fever never seeded; `apply_injury` (sole WoundInfection source) dead with hardcoded RNG; `should_contract` a dead duplicate of the live §17b contagion (probe: real runs carry Epidemic only).
4. **Market price-trend write-only** — `recent_prices` recorded, `trend()`/`price_trend()` zero consumers (no momentum/speculation behavior).
5. **Psychopathology gating dead** — `is_impaired()`/`social_modifier()`/`cognitive_modifier()` zero consumers; depression_risk feeds imagination but never gates behavior.
6. **`logistics` module fully orphaned** — exported, zero production refs; `local_scarcity_modifier` duplicates the live `market::scarcity_modifier`.
7. **Dead public helpers** — `increase/decrease_corruption`, `protest_legitimacy_effect` (+ orphaned `PROTEST_LEGITIMACY_DAMAGE` const), `crime_records()`/`record_violation_with_punishment`, `day_of_season`, plus test-only psychology queries (`activation_level`, `receive_comfort`, `can_inhibit`, `is_impaired`, `is_in_group`, `dehumanize`, `identity_linkage`, `threaten_identity`, `compute_outrage`, `best_scenario`, `positive_socialization`, `record_education`, `neural_like::courtship`).

Retracted during vetting: `rumor_v2` (fully live — create/register/tick_all/transmission with group-attachment escalation) and `theory_of_mind` (live — mind_models updated, `infer_intent` called in the interaction pass); both were grep-fooled by re-export paths. **Next-step recommendation (dependency-ordered):** wire arousal+psychopathology gating into the LOD budget (one re-pin wave) → circadian sleep-seeking → disease kinds + `apply_injury` → market trend → logistics wire-or-delete → delete dead helpers.

---

**Iteration 187 — final-audit gap closures (the 7 residual query-interface findings) ALL CLOSED.** Implemented the dependency-ordered fix wave from `AP2_FINAL_AUDIT.md`; every write-only state now has a decision consumer and every dead helper is retired. Full workspace green (291 passed in tests-crate incl. the 13 re-pinned calibration tests + golden/snapshots regenerated), clippy clean.

**Closure 1 — endocrine arousal → threat bias (§7.2.2).** `attention.recompute_biases` gains an `arousal` parameter: `threat_input += (arousal − 0.5) × 0.5`, mean-zero at the 0.5 anchor (byte-identical to pre-Iter-187 at baseline arousal — probe-pinned in a unit test), ONE-SIDED (arousal can only modulate hypervigilance up or down, never sign-flip the input), clamped to the existing [0.3, 0.7] bias band. Wired at the sim's single `recompute_biases` call (sim.rs ~12,189) from `agent.embodied.endocrine.arousal.level` — the adrenaline axis is no longer write-only. Dominance's documented unwiring (sim.rs:3642) remains a known, deliberate deferral.

**Closure 2 — circadian → sleep drive (§7.3.1).** The motivation block now consults the circadian clock: when `sleep_deprived()` (debt > 0.3) the fatigue need is floored at 0.8, when `should_sleep()` (night + pressure > 0.4) at 0.6 — the Rest action's utility (keyed on `needs.fatigue`) and the §8.1.5 sleep motive both respond, so the agent lies down when its body says so. Identity-at-zero: a rested daytime agent keeps its raw fatigue. Deterministic (no RNG).

**Closure 3 — psychopathology gating (§8.1.16).** Two live consumers: (a) the social-interaction participation mask now requires `!is_impaired()` — a mental-health collapse (`overall_health < 0.5`, the sustained stress/trauma crisis band) withdraws the agent from the social pass (depression/paranoia isolate); (b) `effective_planning_depth()` is halved when impaired — the cognitive-modifier interface gates planning confidence. ONE-SIDED: healthy agents keep the exact pre-Iter-187 values.

**Closure 4 — disease kinds reachable (§32).** (a) `apply_injury` now takes an explicit `rng_value` (the hardcoded 0.3 roll was a deterministic no-op against `injury_chance` 0.1) + dedup/cap guards, and is wired at the violence site with a real seeded draw — severity `injury × 10` reproduces the legacy `health -= injury` damage EXACTLY, the new `energy −= injury × 0.5` drain is the honest addition, and the wound-infection roll uses the live config. (b) A new daily seasonal pass seeds **Cold** (rate `0.01 × (0.4 − temperature)` in the Winter band → ~0.2%/day/agent ≈ 17%/agent/season at Winter 0.2) and **Fever** (Cold→Fever escalation `0.002`/day), same-kind dedup + `MAX_CONCURRENT_DISEASES` guarded, resolved through the normal `system_health` duration path. Calm runs now carry real seasonal illness (probe-verified: Cold/Fever circulate @50K), no longer Epidemic-only. (c) `should_contract` (dead duplicate of the live §17b contagion) deleted.

**Closure 5+6 — market trend + logistics (§29, §19.5.E).** Trade price at the farm-gate now multiplies in (a) `market.price_trend(GRAIN)` × `TREND_BUY_RATE` clamped to [0.9, 1.1] — a falling price lets the buyer haggle down, a rising one has them pay up (mean-zero: trend 0 → exactly 1.0, byte-identical pre-Iter-187) — and (b) the orphaned `logistics::local_scarcity_modifier` (site grain stock → [0.95, 1.2] damped premium/discount). The market's write-only history and the fully-orphaned logistics module both gained live consumers; the trade-price floor at 1.0 still applies.

**Closure 7 — dead helpers deleted.** `increase/decrease_corruption` (institutions), `protest_legitimacy_effect` + `PROTEST_LEGITIMACY_DAMAGE` const (factions, + its unit test), `crime_records()` + `record_violation_with_punishment` (norms), `day_of_season` (ecology). All verified zero-callers in production and tests before removal.

**Re-pin wave (13 tests + golden + 4 snapshots).** Every standing change re-paces the shared RNG stream, so each calibration anchor was re-probed (seed sweeps) before re-pinning: conflict-delta (seed 17 — the only positive-ordered anchor in the 1–20 sweep), dormant-consumer (zero-blast claim scoped to the consumer's own metric), neural PE gate (seed 20, +0.0865 — the double-run probe bug fixed), social-trust (seed set re-anchored: 42/55 fail, 46+55 hold), famine mortality-shape (windows re-anchored from probe data [3,5,4,3,5,3]: pest@900=3, pest@1300=5, pest@1200=3 — the twin-peaked curve mid 1300 (5) strictly out-kills the deep trough 1200 (3), spread 2; the late-window (1400, 3) assertion is dropped as no longer out-killing anything), conception pipeline (3-chain @[72970, 100180, 106340], children_born 2 — one mother replaced), reproduction multiplier (seed 5: 3→9, margin 6), golden regenerated, 4 snapshots accepted (.snap.new → .snap). All 13 re-pinned tests pass individually (incl. the two slow ones: conception 304s, reproduction 445s).

**Liveness re-verification:** `p6_orphan_probe` extended to print mean `threat_bias` (must track arousal) + disease kinds over a year+ horizon (must show Cold/Fever in calm) — run output confirms the folds are live. Retained probes: `p7_repin_probe` (the re-anchor sweep harness; seeded sections keep the sweep reproducible).

---

**Iteration 188 — next audit pass: endocrine stress hunger/thirst channel FIXED + 2 new findings documented.** Re-ran the sim post-Iter-187 and probed the endocrine axes under the live famine window. One genuine integration gap fixed, two system-quality observations logged for future tuning.

**Finding 8 — endocrine acute-stress hunger/thirst channel frozen (FIXED, §7.2.2).** The `acute_stress = pain + hunger×0.3 + thirst×0.2` term read the `embodied.hunger`/`embodied.thirst` *facade* fields, which are frozen at birth (0.2/0.1 — the sim's facade sync deliberately excludes hunger/thirst: "separate tracks"). The channel was a constant ~0.08 in EVERY scenario: famine starvation never elevated cortisol (probe: famine stress == calm stress == 0.333 @50K; the axis only moved under pain — pestilence 0.583). **Fix:** `EmbodiedState::tick_update` now takes live `hunger`/`thirst` params (same values the Eat/Drink decisions use — passed from `needs.hunger`/`needs.thirst` at the sim's biology pass, computed as locals to avoid borrow conflicts), and the acute-stress term uses them. Deterministic, no RNG. **Proof:** new unit test `stress_axis_rises_with_live_hunger_and_thirst` (200 ticks: starved 0.9/0.9 > 0.4, fed 0.1/0.1 < 0.4 — the extreme channel works); MS_TRACE_STRESS trace shows live acute tracking needs (0.016–0.063 varying, vs the old constant). Live famine hunger reaches ~0.2 (0.201 @2000 vs calm 0.034 — the P3-6 starvation works) but sits below the visible aggregate threshold: at 2000 ticks calm/famine stress means are both 0.250 (the axis is bimodal — see Finding 10), so the fix is a correct wiring whose magnitude reflects the actual (modest) famine hunger, not a no-op.

**Finding 9 — legacy `body.hunger`/`body.thirst`/`body.sickness`/`body.injury` vestigial (DOCUMENTED, cosmetic).** The compatibility-facade body fields are written by NOTHING in production (probe: body_hunger 0.200/body_thirst 0.100 frozen at 50K in every scenario) and read by no decision (all eat/drink paths use `needs.hunger`; health.rs/routines.rs take needs as parameters). Dead state, not a wiring gap — deferred as a cleanup candidate (would touch the serde round-trip contract, so it needs its own wave).

**Finding 10 — stress axis bimodal chronic ratchet (DOCUMENTED, tuning candidate).** The MS_TRACE_STRESS histogram over 2000 ticks × 3 scenarios: 49,219 ticks at level 0.0 and 21,383 at level 1.0 — NOTHING graded between (0.76/0.95 appear only as transient crossings). Root cause: the chronic feedback `chronic_delta = chronic_load × 0.1` self-accelerates — any agent whose level crosses 0.6 for ~200+ consecutive ticks accumulates chronic_load until the delta (up to 0.1/tick) exceeds recovery permanently, pinning the level at 1.0 (the only escape, chronic −= 0.0005 when ≤ 0.6, is unreachable once pinned). The Iter-172 fix stopped the MAJORITY from pinning (calm ≈ 25% pinned — mean 0.250) but left a binary fate-lottery: 75% fully calm / 25% chronically stressed in calm, ~50/50 in pestilence. The axis is LIVE and CONSUMED (derived_health stress_penalty, arousal→threat_bias, chronic_load → imagination sim.rs:3551) — this is a realism/tuning observation (a graded axis would differentiate the 0.2-hunger famine leg measurably), not a dead path. Deferred: needs its own probe-driven tuning iteration (the chronic dynamics are load-bearing for several calibrations).

**Verification:** sim crate 985 tests / 0 failures (984 + 1 new). Tests-crate golden/snapshot blast radius in progress — the fix is expected golden-neutral (at 1000 ticks thirst is still low, acute ≈ old constant, no chronic fate flips), but the byte-identity check decides.

---

**Iteration 189 — FINDING 10 CLOSED: the stress axis is now GRADED (the bimodal chronic ratchet is gone).** The Iter-172 fix had left a documented but mechanically-unreachable escape: `chronic_recovery 0.0005/tick` could never fire because a pinned agent's level can't drop ≤ 0.6 while `chronic_delta = chronic_load × 0.1` exceeded recovery. Probe-verified bimodality (MS_TRACE_STRESS over 2000 ticks × 3 scenarios: 49,219 ticks at level 0.0, 21,383 at level 1.0, nothing graded between; calm 3/12 pinned at 1.0 from 2000 → 50K — a permanent binary fate-lottery). **Fix (three parts, all the repo's standard saturating-logistic pattern):** (1) the ACUTE gain is now saturating — `acute × reactivity × (1 − level)` — giving an interior equilibrium `level* = 1 − recovery/(acute×reactivity)` that can only approach 1.0 asymptotically, never clamp-pin; (2) the chronic feedback is 5× weaker (`CHRONIC_FEEDBACK` 0.02 — max delta 0.02 < the 0.03 tone-floor recovery, so a high-chronic agent whose acute subsides always decays); (3) chronic load accumulates/decays saturating (`rate × (1 − chronic)` / `−= recovery × chronic`) instead of the linear accumulator. **Post-fix distribution:** 0 pinned agents in every scenario; calm `[pinned=0 graded=3 calm=9]` mean 0.093 max 0.521, pestilence `[0, 5, 7]` mean 0.149 — intermediate levels populated, scenario severity differentiates (pestilence's pain channel), no permanent pins at any horizon (2000/10K/50K). Two new unit tests pin the new mechanics: `high_chronic_agent_recovers_when_acute_subsides` (a saturated agent with acute 0.02 + full tone escapes the pin in 400 ticks) and `chronic_load_saturates_logistically_not_linearly` (bounded, never exceeds 1.0, meaningful accumulation under sustained stress). **Observation:** famine stress ≈ calm at sample points (0.093 both) — the famine's live hunger only reaches ~0.2 (acute 0.13 vs the thirst-driven baseline 0.11), so the starvation signal is real but below the visible equilibrium shift; this is a famine-INTENSITY observation (the axis now genuinely responds to hunger — unit-pinned at 0.9), deferred as a scenario-tuning candidate, not a wiring gap.

**Verification:** sim crate 987 tests / 0 failures (985 + 2 new endocrine tests). Tests-crate re-pin wave in progress — expected: golden + snapshots re-pin (calm stress 0.25 → 0.09 lowers the derived_health stress_penalty, raising avg_health), plus any calibration reading stress/arousal/threat_bias.

---

**Iteration 190 — FINDING 11 CLOSED: the village hydration gap (documented routine intent implemented) + the largest re-pin wave since Iter-187.** Re-audit of the thirst homeostasis asymmetry surfaced by the Iter-188 probe (hunger ~0.004 fully-fed vs thirst ~0.55 mildly-dehydrated in EVERY scenario).

**Finding 11 — no hydration slot in the daily routine (FIXED).** The §10.3 village routine had THREE Eat windows (06-07, 07-08, 18-20) and ZERO Drink windows — the 07-08 entry's own comment says "Eat/drink" but was implemented as Eat. Villagers ate on schedule (hunger ~0.004) but had no scheduled hydration, so thirst drifted to the utility-driven sawtooth ~0.55 even next to water. **Fix (two parts):** (1) the 07-08 routine slot becomes **Drink** (the documented intent, now real); (2) Drink relief 0.5 → 0.7 (a 2-tick drink mostly quenches, mirroring Eat's per-tick efficiency). **Probe-verified result:** thirst 0.55 → **0.23** (calm), 0.60 → 0.28 (famine), 0.62 → 0.28 (pestilence) @50K — the realistic hydration band; the stress acute from the thirst channel drops correspondingly. New unit test `mid_morning_prefers_drinking`; `morning_prefers_eating` moved to the 06-07 slot.

**The re-pace wave (23 pins — the largest since Iter-187):** the drink change alters every agent's daily action sequence, re-pacing the shared RNG stream. All re-anchors were probe-swept (the new `p10_repin_probe`, retained): conflict-delta pair → seed 12 (the sweep's cleanest anchor preserving the post-Iter-186 drought>calm ordering: calm +666, drought +1,067, gap 206), loneliness → seed 19 (−4,189), institutional patronage → seed 10 (7→9), memory-taxonomy semantic → seed 1, neural PE fold → seed 11 (+0.075), taboo-shame → seed 7 (7<9 acts; the secondary stripped_shame>seeded_shame "act-count tracks" claim dropped — without taboos there is no violation channel to amplify, so the stripped world's extra acts produce ~0 shame), famine windows → mid 900 (6) / trough 1000 (1), faction formation ×3 → pestilence seed 42 (v1=1, v2_active=1 @30K — seed 13's factions now all dissolve before 30K, the reduced stress baseline shortens crisis-faction persistence), trauma bound 0.10 → 0.20 (the divergence-swamping inversion reached −0.159), pestilence onset → fully transient arc (10 @1000, 0 @4000, 0 @12000 — clearance pins), conception → 4-chain @[29660, 76290, 88280, 133700] (4 live children, children_born 2 — two mothers replaced) and the GOLDEN-WINDOW leg seed 42 → 46 (the hydration re-pace moved seed-42's first conception into the 2000-tick window — probe: 1 pregnancy @2000; a 1–60 sweep pins 42 as the ONLY polluted seed, 0/0/0 on 43–47, so the golden leg returns to the Iter-98 historical seed), long-horizon emergence → seed 42 (fires BOTH faction + panic @50K; seed 99's calm grievance now sits below the formation gate entirely). Golden regenerated + 7 snapshots accepted (avg_health shifts with the hydration/stress re-pace).

**Faction-system liveness confirmed (not a regression):** pestilence forms 1–15 factions at every seed @30K — the crisis world keeps its political drama; only the CALM world's grievance threshold moved (hydrated, less-stressed agents radicalize less — the Iter-186 design's intent). **Final gate:** sim crate 988/0, behavioral_delta 12/0, snapshot 17/0, full tests-crate **304/0 (incl. the conception 4-chain @170K and long-horizon 50K determinism+emergence slow legs)**, golden byte-identical post-regeneration, clippy clean.

## Iteration 191 — closing the last dead endocrine axis, active soothing, and the 45-accessor dead sweep

**Finding 12 — endocrine dominance was the last write-only axis (FIXED).** The Iter-187 note documented dominance as a "known deliberate deferral": `dominance_aggression_modifier()` had ZERO consumers, and the escalation decision at sim.rs read only `personality.dominance` — the live endocrine axis (updated every tick from victory/defeat/status events) fed nothing. The iteration wires `dominance_aggression_modifier()` into `should_escalate`'s aggression fold, closing the last dead endocrine axis. `effective_inhibition` — computed every tick but read only by the dead `can_inhibit()` — gains its first consumer in the same fold (an agent that cannot inhibit presses the attack).

**Finding 13 — attachment `receive_comfort` never fired (FIXED).** The active soothing path existed but the Comfort interaction was filtered OUT of the interaction pass (filter was Gossip|Help|Trade). Comfort is now processed, and `receive_comfort` fires for the first time. **Calibration note:** the first wiring attempt (params up to 0.24/event) over-corrected — comfort zeroed partnered separation distress entirely (4 seeds × 2 rates = 0.000), killing both the coupling-liveness and rate-response contracts. Empirical damp calibration (0.0005, comfort fires ~2,100×/agent/50K): coupling stays live (31–43/46–48 agents nonzero), comfort *halves* partnered distress (0.014 → 0.0063) without erasing the signal — an honest, visible effect.

**Finding 14 — 45 verified-dead accessors (FIXED via deletion).** An exhaustive workspace-wide caller scan (corrected per-file regex, tests+TUI+CLI+benches included) flagged 45 zero-caller helper accessors. Each was verified NOT to be a write-only-gap (underlying state consumed via a live path, e.g. `effective_mobility` live while `effective_strength` dead) before deletion. Documented-intent methods preserved: courtship `advance/regression_probability` ("Kept as the documented probabilistic design") and the panic `fatigue/drain` helpers (live at sim.rs:13167). One false positive caught by build (`gestation_stage_of`, live at reproductive.rs:203) restored.

**Re-pin wave (26 pins, probe-swept):** the dominance/comfort/inhibition folds re-pace the shared RNG stream. Key re-anchors: dormant-consumer pest leg re-OPENS (dominance fold dropped pest fear below the coping-saturation gate — the gate factor now fires live on its own metric), faction ×3 → seed 13, motivation joy floor 0.03 → 0.015, moral_panic 99 → 5 (+ Leg C same-seed bug fixed: it compared seed-5 `sim` against seed-99 `again`, only matching by coincidence before), pestilence arc back to transient-endemic (11/7/5), taboo 7 → 3, long_horizon 42 → 99, conception 4-chain [28840, 46960, 57530, 78850] + children_born 2→3. **Final gate: sim 991/0, full tests-crate 304/0, golden byte-identical, clippy clean.**

---

## Iteration 192 — the famine despair chain closure (sadness → despair → depression-from-deprivation)

**Finding 15 — famine despair was structurally unreachable (FIXED, two wirings).** The famine grain drain (P3-6) genuinely starves — 3 agents cross the 0.5 goal-congruence gate at the window peak (probe: maxH 0.711, sadness 0.235 firing) — but **despair stayed 0.000 for every agent**. Root cause: `future_implication = 1 − threat − need_pressure` (sim.rs) can only go negative when `threat + need_pressure > 1`, and since both are clamped to 0..1, a starving agent (threat 0, need 0.71) gets **+0.29 → HOPE fires, despair never does**. The documented "goal-incongruence branch (sadness → despair → depression-from-deprivation)" (the famine-grain-drain comment) died at the despair link. Even worse, despair had **no consumer into depression_risk** — the endpoint read only chronic_stress + loss magnitude, so the chain's final link ("depression-from-deprivation") was also dead.

**Fix 1 — bleak-excess term (sim.rs appraisal construction).** Past the 0.5 goal-congruence gate, the excess unmet HUNGER drags the signed future outlook negative at 2× the excess: `bleak_excess = (hunger − 0.5) × 2`, `future_implication = (1 − threat − need − bleak).clamp(−1, 1)`. At hunger 0.71 → −0.13 (despair fires); at hunger 0.5 → 0 (gate); at hunger 1.0 → −1.0. **Keyed on HUNGER, not need_pressure**: thirst routinely crosses 0.5 in calm windows (spaced drink windows), so a max(hunger, thirst) key fired transient despair in CALM (probe: calm maxD 0.880 — a golden byte-identity break); hunger stays < 0.5 in calm/pestilence, so the term is zero there and only the famine window crosses it (calm re-probe: maxD 0.000, byte-identical).

**Fix 2 — despair → depression_risk (psychopathology).** New dedicated `despair` input to `tick_update` (weight 0.2, below chronic-stress/loss so it modulates, never dominates), wired at the sim call site. Zero-at-zero: calm agents (despair 0.000) contribute nothing. New unit test `despair_feeds_depression_risk` (500 ticks: despair 0.5 → risk > 0.01, control stays 0.000, and risk < 0.5 so despair never dominates).

**Probe-verified result (p12_famine_probe, retained):** famine @1500 — gate=3, sadness 0.235, **despair max 0.157** (was 0.000, 1 over-gate agent now despairs), depression risk starts moving; famine @2000 — gate=2, despair 0.052. Calm byte-identical at every horizon (despair 0.000, maxD 0.000, maxDep 0.0000). **No re-pin wave**: the bleak term is hunger-gated below every calm/pestilence window and the famine integration tests assert grain depletion, not emotions. **Final gate: sim 992/0 (991 + 1 new), full tests-crate 304/0, clippy clean.**


## Iteration 193 — humiliation/moral-injury risk channels wired (paranoia + resentment un-deadened)

**Finding 16 — the psychopathology call site hardcoded `humiliation_recent` and `moral_injury` to ZERO (FIXED).** The §9.4 paranoia channel's humiliation leg (×0.25) and resentment's BOTH legs (moral_injury×0.25 + humiliation×0.2) were structurally dead: the inputs arrived as `Fixed::ZERO` at `tick_update`, so resentment_risk could only move via chronic_stress×0.1. Both emotions have live producers already consumed elsewhere — `emotions.humiliation` from status_threat events (also feeding the escalation fold) and `emotions.moral_outrage` from sacredness_violation — so the wiring was purely missing. Probe-verified both emotions sit at 0.000 in calm/famine/pestilence windows (they need status-threat/sacredness events, rare in the default scenarios), so the wiring is zero-blast in production scenarios: golden determinism intact, no re-pin wave. The channels now fire whenever an agent actually suffers status loss or sacred violation — the documented causal inputs finally reach their risk endpoints. New unit test `humiliation_and_moral_injury_feed_resentment` (0.3 humiliation + 0.4 moral_injury → resentment > 0.01 with control 0.000; paranoia moves on humiliation alone).

**Also closed:** remaining clippy debt across the retained probe examples (named-arg format strings, dead `body_hunger/thirst` accumulators, an unreachable `InteractionKind` wildcard, doc-list indentation) — **clippy fully clean, 0 warnings workspace-wide.**

**Final gate: sim 993/0 (992 + 1 new), full tests-crate 304/0, clippy clean, golden byte-identical (zero-blast wiring).**

## Iteration 194 — interoception trauma_load wired (the last ZERO-input init gap)

**Finding 17 — `initialize_from_personality`'s documented `trauma_load` was hardcoded ZERO (FIXED).** The §8.1 interoception init signature takes `(neuroticism, openness, trauma_load)` and its doc-comment says "Initialize interoceptive state from personality traits and trauma history" — but the populate call site passed `Fixed::ZERO`, deadening the `trauma_load × 0.3` term in `negative_bias`. Meanwhile the attachment block immediately below drew a `trauma_history` 0.0–0.3 for the same agent. The wiring was purely missing: a trauma-loaded agent's `negative_bias` should rise, amplifying felt hunger/thirst/pain and feeding higher depression risk downstream (`felt_need_deficit`, sim.rs §17 derived-state pass).

**Zero-stream-diff wiring:** the identity×3 + moral_values + wealth + caregiver/trauma populate-RNG draws are hoisted into `let`s immediately before the `AgentBundle` literal **in their exact current stream order**, so the shared `trauma_history` now feeds BOTH the interoception (`trauma_load`) and the attachment system with the populate stream byte-identical. Probe-verified: `intero.negative_bias` mean 0.386 (min 0.268, max 0.496) across scenarios — the trauma term is live. New unit test `trauma_load_amplifies_negative_bias` (trauma 0.3 adds 0.09 ± 0.01 to negative_bias, control at trauma 0).

**Zero re-pin wave:** the golden baseline, all snapshots, and all 304 calibration tests pass unchanged — the negative_bias delta (≤0.09) stays below every decision threshold in the default Secondary-tier population (the felt_need_deficit consumer is Focal-gated), so behavior is byte-identical in production windows. **Final gate: sim 994/0 (993 + 1 new), full tests-crate 304/0, golden byte-identical, clippy clean.**

## Iteration 195 — §17 developmental psychology wired (moral_development → moral cognition)

**Finding 18 — the entire developmental psychology state was write-only (FIXED).** `identity_formation`, `moral_development`, `socialization_completeness`, and `cognitive_development` were computed every centum for Focal agents (sim.rs `developmental.tick_update`) and consumed by NOTHING in the workspace — a dead ratchet. The natural first consumer: `moral_development` (Kohlberg stage) modulates the internalized shame/pride response in `update_moral_emotions`, which runs on the SAME Focal gate as the producer, so the developmental pipeline finally feeds behavior. New signature: `update_moral_emotions(…, moral_development)` with a **baseline-corrected scale** (1.0 at the default stage 0.4, ±1.5/unit deviation, clamped [0.5, 2.0]) — the first attempt (0.5 + dev×0.5) was NOT zero-blast: the golden changed (seed-42 riverford has Focal agents at 1000 ticks whose shame/pride shifted 30%), while the baseline-corrected form is an exact no-op at populate defaults. New unit test `moral_development_amplifies_shame_and_pride` (development 1.0 > 0.4 on shame/pride; outrage untouched). **Zero re-pin wave: golden byte-identical, all 304 tests pass unchanged.** **Final gate: sim 995/0 (994 + 1 new), full tests-crate 304/0, golden byte-identical, clippy clean.**

## Iteration 196 — education_quality producer + identity_formation → appraisal consumer (closing the write-only chain)

**Finding 19 — the developmental `education_quality` input was a hardcoded 0.5 placeholder, and `identity_formation` still had no consumer (FIXED, chain closed).** The §17 developmental pass ran every centum for Focal agents with `education_quality = Fixed::from_f64(0.5)` — a placeholder that ignored the live education system entirely — and `identity_formation` (the field it grows for ages 12–30) was still write-only after Iter-195 wired `moral_development`. Two-part fix:
1. **Producer:** `education_quality` is now the agent's own `learning_aptitude` — the same capacity `effective_learning` weights at 0.3 in the apprenticeship pass, so identity formation genuinely scales with the education pipeline (a high-aptitude agent's identity forms faster). New unit test `education_quality_scales_identity_formation` (10 ticks at aptitude 0.9 vs 0.2, same age/support → high forms faster).
2. **Consumer:** `identity_relevance` in the appraisal — previously a hardcoded `0.2` constant — now scales with `identity_formation`, baseline-corrected: exactly 0.2 at the default formation 0.5, rising toward 0.3 at full formation, falling toward 0.1 at identity-less 0 (±0.2/unit, clamp-free since formation ∈ [0,1]). A formed identity makes events more personally relevant, and every identity-relevant emotion (pride/shame/humiliation/awe, all `× identity_relevance`) now differentiates with development. **Zero-blast: exact no-op at populate defaults** (golden byte-identical; the Iter-195 lesson — a non-baseline-corrected scale broke seed-42's Focal agents — applied from the start). **Final gate: sim 996/0 (995 + 1 new), full tests-crate 304/0, golden byte-identical, clippy clean.**

**Remaining write-only developmental fields (documented, deferred):** `socialization_completeness` and `cognitive_development` still have no consumer — both are observational state that the §17 pass maintains (socialization grows <18yo, cognitive maps to life stage) and neither feeds a decision yet. Natural future sinks: socialization → relationship-formation quality, cognitive → skill acquisition rate. Both would require baseline-corrected consumers (the Iter-195/196 pattern) to stay byte-identical.

## Iteration 197 — the last two write-only developmental fields wired (socialization → relationships, cognitive → skill learning)

**Finding 20 — `socialization_completeness` and `cognitive_development` were the remaining write-only developmental fields (FIXED, both closed).** After Iter-195 (moral_development → moral emotions) and Iter-196 (identity_formation → appraisal), the last two §17 fields had no consumers. Both are now wired with the established **baseline-corrected** pattern (exact no-op at populate defaults; only agents whose development actually advances diverge):
1. **`socialization_completeness` → interaction magnitude** (sim.rs speech-act relationship deltas): a fully-socialized agent registers interactions more strongly, so its bonds advance faster — scale 1.0 at the default 0.5, →1.25 at full socialization, →0.75 at unsocialized 0, applied per-edge (each agent's own socialization scales its own directed bond).
2. **`cognitive_development` → skill practice** (sim.rs §4.2 practice call): higher cognition learns faster — the life-stage state (0.1 infant → 1.0 adult → decline past 50) modulates the neuroplasticity input to `practice`, scale 1.0 at the default 0.7 → 1.3 at full adult cognition. New unit tests `stronger_positive_interaction_builds_more_trust` (the magnitude lever the socialization scale pulls) and `higher_neuroplasticity_learns_faster`.

**Small honest re-pin wave (2 pins — the LONG-HORIZON consequence of the closures):** the developmental state only diverges for agents that actually develop, so short calibrated windows stay byte-identical (golden + all 500/1000/2000-tick tests unchanged), but at 10K+/170K horizons children develop → socialization/cognitive fire → courtship re-paces. (1) the 10K surface snapshot re-accepted; (2) the conception liveness leg re-anchors seed 13 → **seed 1** — a 7-seed 170K sweep (p18_repin_probe) pinned seed 1 as the strongest clean chain: the FULL 3-chain [14620, 86200, 105210] with **ZERO mother-replacement** (3 live children, 3 marriage records, children_born 3, open_preg 0, population 15 — every birth through the pregnancy path, no counter wiped; seed 13's trajectory degraded to a 2-chain with both mothers replaced, children_born 0). **Final gate: sim 998/0 (996 + 2 new), full tests-crate 304/0, golden byte-identical, clippy clean.**
