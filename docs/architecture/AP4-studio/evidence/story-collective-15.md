# Collective 15 (STORY 15, 2026-08-31)

Owner: STORY. Generated 2026-08-31 against `1241eda` (86/106).
Status: **DRAFT — blocked on WP-I, Era III full verbs after.**

## Why blocked

`STORY 15` is `collective` full verbs — `Group`/`Institution`/`World` as `GrossReferent` driving `4-quadrant` pathology via `CollectiveField` `WP-I`. Requires `mindstrata-development/src/collective.rs` with `all_lines()` ordering + `CALIBRATION-PENDING(AP3)` constants, same dependency as `STORY 14`. Until `WP-I` lands, `STORY 15` is `ponytail:` doc-closed same as `14`.

## What lands this note

* Records that `STORY 3` remaining (`15` only — `14` now at `story-collective-behavioral.md`) is blocked, not debt — same batch as `SIM` glue + `DESIGN` final CO + `CLIENT`/`TOOLS` converge tail.
* Era III pure types `template.render` + `extract_referent` + `RealmTriple.is_legal` are `69/69` green prerequisites, so `15` can land as pure types before sim wiring when `WP-I` lands.

## Next

Land with `WP-I` — same batch as `SIM 1-2` detangle and `DESIGN 4.24` real `CO-2026-003`.
