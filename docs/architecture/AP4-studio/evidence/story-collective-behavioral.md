# Collective behavioral (STORY 14, 2026-08-31)

Owner: STORY. Generated 2026-08-31 against `6d6c031` (80/106).
Status: **DRAFT — blocked on WP-I, shape filed.**

## Why blocked

`STORY 14-15` collective behavioral needs `mindstrata-development` `collective.rs` WP-I — `CollectiveField` today is `inert` (`dev/collective.rs` scaffold @`21aaaf1`). Wiring `Threat/Transgression/Bond/Grief → dark/golden × addiction/allergy` is the `4-quadrant` fan-out already proven at `SIM 12-13` `CO-2026-001`, but `collective` as `GrossReferent::{Group,Institution,World}` needs WP-I's `all_lines()`-ordered field.

## What lands this note

* Records that `template.render` + `extract_referent` + `RealmTriple.is_legal` are the Era III pure-type prerequisites — all `69/69` dev green, `golden 5/5`, so STORY 14 can be pure-type work before sim wiring.
* When WP-I lands, STORY 14-15 will be `dev/collective.rs` + `sim/collective.rs` wiring with `CALIBRATION-PENDING(AP3)` constants, `golden 5/5` byte-identical if pure, `CO-` if behavioral.

## Next

Land with `WP-I` — same batch as `SIM` institutions detangle and `DESIGN 4.24` `CO-2026-003`. Until then, `STORY 3` remaining is `ponytail:` blocked.
