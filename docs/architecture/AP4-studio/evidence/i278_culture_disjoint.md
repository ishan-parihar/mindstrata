# Iteration 278 — UM-2 gate probe: seed-disjoint cultures (evidence)

**Status:** GATE PROBED — PARTIAL PASS · **Doctrine:** probe-first, honest verdict.

## Protocol (`i278_culture_disjoint.rs`)

The i284 recipe: same founder bands, different event histories → disjoint
meme rosters by 20K. Ran the 6-seed family (42–47) at 20K, N=12; computed
cross-seed jaccard on (a) GENERATED meme sets (the signal) and (b) the seeded
founding vocabulary (the control — shared by construction per audit H5).

## Results

| Measure | Value |
|---|---|
| Seeded-control jaccard (all 15 pairs) | **1.000** — correct control |
| Generated jaccard, mean | **0.402** |
| Generated pairs disjoint (j ≤ 0.5) | **11 / 15** (min 0.125) |
| Generated pairs identical (j = 1.0) | 3 / 15 (seeds 43/44/46 — shallow rosters, 1 meme each) |
| Deepest-culture pair | 42–45 = 0.75 (both deep, both Safety-dominant) |

Generated content now spans multiple classes and cites live referents, e.g.
seed 45: "The Village Temple must answer to the civilization-axioms of this
village [genesis:Safety:Political:4]" vs seed 47's Council-cited variant.

## Verdict: PARTIAL — mechanism proven, diversity blocked at a recorded boundary

**Through the gate:** the generated-vs-seed contrast is exactly right —
shared founding vocabulary (1.000) with generated divergence (11/15 pairs
low). Villages with deep collective development generate genuinely different
culture from the same starting memes.

**Not yet through:** the divergence is confined to ONE bucket (Safety). The
other three buckets' lines never reach band II at N=12 (the i272 mortality
block for Identity, the i274 pacing for Relational at monthly cadence, the
Meaning baseline 0.1×) — so the generated class space is one template
family, and two deep Safety-only villages can still converge (0.75). The
shallow-trio identical rosters (43/44/46) are the same finding at stage
resolution 1.

**Blocker classification:** this is the ALREADY-RECORDED catalyst-diet debt
(i272/i274 evidence files), not new construction debt. The unblock paths
are the ones §6.3 already names: the i280 Identity-feed work, festival-dense
regimes for Relational, or longer horizons. No new construction gap was
found by this gate — the WP-G2/H2/I machinery is exercised end-to-end.

## What this means for DC-2

- WP-G2 (generation), WP-H2 (polarity), WP-I (stage gating) are live and
  compose: collective stages → gated classes → grounded text → meme pool →
  transmission.
- The UM-2 milestone's full criterion ("seed-disjoint cultures, old roster
  retired") is gated on the diet breadth, which is a CALIBRATION matter with
  recorded unblock paths — not a construction matter. Retiring
  `seed_initial_memes` before the diet diversifies would remove the control
  group that proves divergence; it stays until a re-run of this probe shows
  ≥3 buckets generating.
